# 2026-07-10 작업 일지

## 요약

연간 점검계획 **기준일 규칙 변경**(점검시작일 우선) 및 **FIRE-S1 시스템 테스트 통과**. 커밋 2건, 프로덕션 미배포.

## 1. 샤브올데이 "정기 미조회" 문의 → 버그 아님 확인

- DB 확인 결과 샤브올데이는 사용승인일(2015-07-27)과 정기 항목 모두 존재 (2026·2027년 각 12건 = 24건)
- 7월에 정기가 안 보인 이유: **7월이 특별점검 월**이라 정기 대신 special_작동 배치 (연 12건 설계) + 해당 특별 항목은 7/6 점검과 연결되어 completed 상태
- 특별월 정기 없음은 **현행 유지로 확정** (사용자 결정)

## 2. 기준일 규칙 변경 — 커밋 `e2569a6`

기준일 = **최초 점검시작일(inspections.inspection_start_date) 우선 → 없으면 사용승인일** (사용자 확정)

| 파일 | 변경 |
|---|---|
| `erp/src/lib/inspection-plan-generator.ts` | `loadAnchorDates()` 헬퍼 신설(기준일 단일 소스), 지난 달 정기 생성 생략 |
| `erp/src/app/api/cron/generate-yearly-plans/route.ts` | 사용승인일 없는 고객도 크론 대상 포함 |
| `erp/src/app/(dashboard)/inspections/actions.ts` | 사용승인일 없는 종합/작동 고객의 점검 등록 시 연간 계획 즉시 자동 생성 |
| `erp/src/app/(dashboard)/customers/actions.ts` | 사용승인일 변경/삭제 시 예정일 재계산도 동일 기준일 |
| `erp/src/app/(dashboard)/inspection-plans/page.tsx` | 미점검 초과 판정 동일 기준일 + 일반관리 판정 제외 |
| `erp/src/app/(dashboard)/inspection-plans/actions.ts` | 스마트 제안·전월 복사도 동일 기준일, 제안 사유에 출처 표기 |

## 3. FIRE-S1 시스템 테스트 통과 — 커밋 `d516031`

- 항목: 고객 등록 → 연간 계획 자동 생성 (Victory10_entire.json)
- 자동 테스트 스크립트 `erp/scripts/test-fire-s1.mts` — **13개 체크 전부 통과**
  - 작동/종합 생성 구성, 점검시작일 폴백·우선순위, 지난 달 정기 생략, 멱등성, 일반관리·무기준 0건
  - 실행: `npx tsx scripts/test-fire-s1.mts` (dev DB, 테스트 데이터 자동 정리)
- 체크리스트 현황: **85 passed / 40 pending / 17 partial** (142개)

## 내일 할 일

1. **FIRE-S2** (계획 확정 → 6단계 자동 계산 → 점검업무 생성) — 서버 액션이 세션 인증 요구, 브라우저 구동 검증 필요
2. **FIRE-S3** (단계 완료 → 모니터링·달력 반영) — 동일
3. 기준일 수정분 **프로덕션(sjfire.co.kr) 배포**
4. (대안) 자동화 가능 항목: NF-PERF-1 대량 목록 응답

## 메모

- sync-checklist 훅은 TaskUpdate의 `tool_input.subject`를 읽음 → **status 변경 시 subject도 함께 보내야 동기화됨**
- DB 제약: `customers.created_by` NOT NULL, `inspections.year`는 생성 컬럼(insert 불가)
