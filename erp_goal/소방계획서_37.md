# 소방계획서_37 — 설비 대장 세부제원 패널 확대 (창 넓히기 · 글자 한 단계)

작성: 2026-08-30
성격: **구현 완료 — 독립 판정 전.** Json_Rule 규칙 3에 따라 전 criteria `code_present` 이하.
집계: 4섹션 **29 criteria** — `code_present` 25 · `pending_order` 4 · **`implemented` 0**
관련: [소방계획서_35](소방계획서_35.md)(글자 배율 토큰·`--fs-scale` 인프라 — 이 문서는 그 위에 얹는다), [소방계획서_28](소방계획서_28.md)(점검표 입력구 단일화), [소방계획서_26](소방계획서_26.md)(설비별 ○×／ 입력)

---

## 0. 요구와 화면

> "f:\AI\ERP\erp_goal\image-22.png 세부제원을 더 크게 창을 만들고, 폰트 더 크게 제안해줘"

대상은 고객 상세 → 소방계획서 탭 1.4에서 **설비명을 클릭하면 열리는 우측 슬라이드 패널**
(`plan-form14.tsx` "설비 대장 — 세부 제원"). 별지 9호 4~7쪽 · 별지 4호 3~7쪽에 인쇄되는
제원의 **단일 원본**이다.

### 왜 좁았나

변경 전 폭은 `min(92vw, 640px × --fs-scale)`. 1920px 화면에서 **보통 배율이면 640px**이고,
`p-4`를 빼면 실가용 608px이다. 그 안에 들어가는 것:

- 3-1~3-8 아코디언 8개
- 6열 rowtable(동형 / 용량 / 설치개수 / 보 확산소화기 / 비고 …)
- [문서 미리보기 나란히]를 켜면 `md:w-1/2`로 다시 반 → **한쪽 약 300px**

### 사용자 결정 (2026-08-30, AskUserQuestion)

| 축 | 선택 |
|---|---|
| 창 크기 | **기본 폭 상향 + [넓게] 토글** (드래그 리사이즈·전체화면 모드는 기각) |
| 폰트 범위 | **이 패널만 한 단계 크게** (전역 기본 배율 상향·토큰 실값 상향은 기각) |

---

## 1. 결정

### D37-1 — 확대는 **패널 스코프**다. 전역 토큰(`--fs-1~6`)에 손대지 않는다

35가 만든 `--fs-1~6`은 소방계획서 서식 화면 **전체**의 실값이고 `@media print`가 되돌리는
목록과 한 쌍이다. 여기를 올리면 이미 만족스러운 1.1~3장·목차까지 함께 흔들리고,
`test-plan-readability`의 S0 고정 기준선(픽셀 불변 증명)을 통째로 다시 떠야 한다.

대신 **배율만 패널 서브트리에서 한 단계 올린다**. 되돌리기는 CSS 한 줄이다.

### D37-2 — `--fs-step`은 `--fs-scale`의 사본이다 (변수 순환 회피)

부스트를 이렇게 쓰면 **안 된다**:

```css
[data-fs-boost] { --fs-scale: calc(var(--fs-scale) * 1.15) }   /* ✗ 순환 */
```

CSS 커스텀 프로퍼티가 자기 자신을 참조하면 **guaranteed-invalid**가 되어 값이 통째로 죽고,
글자가 상속 기본값으로 떨어진다. 조용한 전멸이다. 그래서 읽을 이름을 하나 더 둔다.

```css
html[data-fs="lg"] { --fs-scale: 1.15; --fs-step: 1.15 }
html[data-fs="xl"] { --fs-scale: 1.3;  --fs-step: 1.3 }
[data-fs-boost]    { --fs-scale: calc(var(--fs-step) * 1.15) }
```

사본이라 어긋날 수 있으므로 `test-font-scale`이 **두 값을 대조한다**(B-6).
그리고 `--fs-scale`을 각 규칙에서 **먼저** 쓴다 — `test-font-scale.mts:50`이 `{` 바로 뒤를
정규식으로 읽기 때문이다. 순서를 바꾸면 배율 검사가 조용히 `undefined`를 본다.

### D37-3 — 폭은 글자와 **같은 수**로 커진다

35가 세운 불변식이다(`plan-form14.tsx` 패널 주석): 숫자열 폭(`--fs-col-num`)이 배율과 함께
넓어지므로, 글자만 키우고 폭을 고정하면 표가 패널을 넘겨 가로 스크롤이 생긴다.

그래서 폭도 **부스트된** `--fs-scale`을 곱한다. 패널 요소가 `data-fs-boost`를 직접 갖고 있으므로
그 요소 자신의 `width` 선언에서도 부스트된 값이 풀린다.

```css
[data-spec-panel]            { --fs-panel-w: 900px }
[data-spec-panel][data-wide] { --fs-panel-w: 1400px }
```
```
w-[min(96vw, calc(var(--fs-panel-w) * var(--fs-scale)))]
```

**실측 폭** (1920px 화면):

| 배율 | 기본 (900px) | [넓게] (1400px) |
|---|---|---|
| 보통 md | 640 → **1035px** (실측 확인) | 1610px |
| 크게 lg | 736 → 1190px | 1843px (96vw 상한) |
| 아주 크게 xl | 832 → 1345px | 1843px (96vw 상한) |

> 기본이 900이 아니라 1035인 것은 글자가 15% 커진 만큼 폭이 따라간 결과다.
> 너무 넓으면 `--fs-panel-w: 780px`로 내리면 보통 배율에서 ~897px — globals.css 한 줄이다.

96vw 상한은 유지한다. 좁은 화면에서 화면을 통째로 덮으면 뒤 본문 맥락이 사라진다.

### D37-4 — 인쇄는 부스트도 되돌린다 (D35-5 연장)

법정 서식은 규격이라 글자가 커지면 칸을 넘친다. `@media print`에서 **`--fs-scale`을 직접**
되돌려야 한다 — `--fs-step: 1`만으로는 `[data-fs-boost]`가 여전히 ×1.15를 곱해 결과가 1.15다.

```css
@media print {
  :root, html[data-fs] { --fs-scale: 1; --fs-step: 1; … }
  [data-fs-boost] { --fs-scale: 1 }     /* ← 명시도가 같으므로 소스 순서가 뒤여야 이긴다 */
}
```

이 규칙을 위로 옮기면 조용히 무력해진다. 주석과 검사(B-7)로 함께 못 박았다.

### D37-5 — [넓게] 선택은 localStorage다 (DB 아님)

글자 배율(`profiles.form_font_scale`, 마이그레이션 154)과 달리 **기기별 취향**이다.
27인치 데스크톱에서 [넓게]를 골랐다고 노트북까지 따라갈 이유가 없다. 마이그레이션 0건.

⚠ 초기값에서 `localStorage`를 읽으면 서버 렌더에 없어 하이드레이션이 어긋난다.
첫 렌더는 항상 `false`, 마운트 뒤 한 번 교정한다.

---

## 2. 변경 파일

| 파일 | 내용 |
|---|---|
| `erp/src/app/globals.css` | `--fs-step` 사본(:181, :206-207, :226) · `[data-fs-boost]`(:214) · 패널 폭 토큰(:215-216) · 인쇄 역치환(:233) |
| `erp/src/components/customers/plan-form14.tsx` | `SPEC_WIDE_KEY`(:73) · `specsWide` 상태·localStorage(:119-133) · 패널 div 속성·폭(:733-734) · [넓게] 버튼(:738-743) |
| `erp/scripts/test-font-scale.mts` | 정적 B-6·B-7·B-8(:62-73) · 런타임 B-9·B-10·B-11 + 변이 S35B(:150-186) |
| `erp/scripts/test-plan-readability.mts` | `BOOST_OFF`(:200) · `OLD_VALUES`에 합성(:201) · 항등·기준선 모드 주입(:413, :443) |

**DB 변경 없음. 마이그레이션 없음.**

---

## 3. 검증 실측 (2026-08-30)

| 검사 | 결과 |
|---|---|
| `npx tsc --noEmit` | **exit 0 · 오류 0건** |
| 서빙 CSS 신선도 (dev 청크 직접 fetch·grep) | `data-fs-boost` 2 · `fs-panel-w` 4 · `--fs-step` 5 — 스테일 아님 |
| `test-font-scale.mts` | **18 통과 / 0 실패** |
| `test-font-scale.mts --mutate` | **17 통과 / 0 실패** — 부스트를 죽이면 B-10이 무너짐(항진명제 아님) |
| `test-plan-readability.mts --identity` | ⚠ **미완결 — §5 참조** |

측정된 실값(B-10·B-11, 보통 배율):
- 패널 **안** `text-form-xs` = 패널 **밖** × 1.15
- 패널 폭 = `min(96vw, 900 × 1.15)` = 1035px

### 대조군을 함께 둔 이유

`--mutate`는 `[data-fs-boost] { --fs-scale: var(--fs-step) !important }`를 주입해 **부스트만**
죽인다. 그때 B-10이 빨개져야 "검사가 살아 있다"가 증명된다. 초록만 보고는
[[feedback_exhaustive_has_an_axis]]의 58/58 사고를 반복한다.

---

## 4. 내가 틀린 것 두 가지 — 검사가 잡았다

설계 단계의 두 전제가 **실행에서 뒤집혔다**. 다음 사람이 같은 착각을 하지 않도록 남긴다.

### 오판 1 — "`:root{--fs-scale:1!important}`가 부스트를 누른다"

**틀렸다.** `!important`는 그 선언이 **이긴 요소**(`<html>`)에서만 힘을 쓴다. 자식은 그 값을
**상속**할 뿐이고, 상속값은 그 자식에 **직접 매칭되는** 선언(`[data-fs-boost]`)에 진다 —
중요도와 무관하게 상속이 먼저 탈락한다.

→ 항등 축이 실제로 깨졌다. 1.4·1.4-specs 두 화면에 12.65 / 13.8 / 14.95 / 16.1 / 17.25px
(= 구 값 × 1.15)가 나타났다. 조상 요소에 `!important`를 걸어 자손을 통제할 수 없다.

### 오판 2 — "`--identity`가 `OLD_VALUES`를 주입한다"

**틀렸다.** `OLD_VALUES`는 `--print` 축 전용이다(`:306`). `--identity`는 **라이브 화면을 수집해
커밋된 고정 기준선(`scripts/_fixtures/35-baseline.json`)과 대조**한다. 그래서 `OLD_VALUES`만
고쳤을 때 실패 수치가 **한 자도 안 변했다** — 그 사실이 오판의 증거였다.

→ 수정: 수집 루프와 1.4-specs 수집 직전에 `BOOST_OFF`를 주입(`:413`, `:443`).
`--overflow`에는 걸지 않는다(거기선 실제로 커진 기하를 재야 넘침이 의미가 있다).
`--baseline`에는 **함께** 건다 — 기록 때 안 걸고 대조 때만 걸면 기준선을 다시 뜨는 순간
항등 축이 통째로 빨개진다.

> ⭐ 이 구조는 항등 주장을 **더 강하게** 만든다: "패널이 변한 이유는 배율 변수 한 개뿐이고,
> 그 한 개를 되돌리면 옛 픽셀과 완전히 같다." 패널 안 `text-form-*` 클래스를 실수로 바꾸면
> (예: `text-form-xs` → `text-form-sm`) 부스트를 눌러도 히스토그램이 어긋나 여전히 잡힌다.

---

## 5. 잔여 — 착수 전 반드시 읽을 것

### R37-1 (판단 필요) — 항등 기준선에 `12px: 39→40`

`BOOST_OFF` 주입 후 남은 유일한 폰트 차이는 **1.4 화면의 12px 노드 +1개**다.
정체는 새로 추가한 **[넓게] 버튼**(`text-form-2xs` = `--fs-2` = 12px)이다.
타이포그래피 회귀가 아니라 **의도한 UI 추가**다.

→ **권고는 기준선 재기록**: `npx tsx scripts/test-plan-readability.mts --baseline`.

> ⚠ **초안에서 이 대가를 과대평가했었다.** "재기록하면 35 코드모드의 픽셀 불변 증명이
> 사라진다"고 적었으나, `test-plan-readability.mts:8-18`이 명시한다 — **기준선은 S3 적용
> 시점에 이미 한 번 재기준화됐고**, `--identity`는 지금 코드모드 증명이 아니라
> **"앞으로 크기가 실수로 바뀌지 않는가"의 회귀 잠금**이다. 그 증명은 당시 실행 기록과
> `소방계획서_35.json` S2-7에 남아 있지 이 fixture가 지고 있지 않다.
> → **재기록 비용은 낮다.** 잠금을 현재 UI에 맞춰 다시 거는 정상 절차다.

`--baseline`은 `BOOST_OFF`를 함께 걸도록 이미 고쳐 두었으므로 재기록해도 두 모드가
어긋나지 않는다. 실행 자체는 R37-2(dev 재기동 후 완주) 이후 · 사용자 승인 후.

### R37-2 (환경) — `--identity` 실행이 ch3에서 타임아웃

`page.goto … ?form=ch3` 15초 초과로 중단되어 **cover · 1.4-specs가 수집되지 않았다**
(화면 12/15). 코드 회귀가 아니라 dev 서버 지연으로 보인다([[risk_dev_postcss_worker_leak]]).
**dev 재기동 후 재실행**이 선행되어야 R37-1을 확정할 수 있다.

### R37-3 (미실행) — 나머지 게이트 축

`--overflow` · `--print` · `--cls` · `test-plan-tab.mts` 미실행.
특히 `--print`는 D37-4(인쇄 역치환)의 실증 축이므로 반드시 돌려야 한다.

### R37-4 (미실행) — 육안

1. 기본 상태에서 3-1 표가 가로 스크롤 없이 들어오는가
2. [넓게] 누르고 새로고침 → 유지되는가 (localStorage)
3. [문서 미리보기 나란히] + [넓게] 조합에서 좌우가 각각 읽히는가
4. 패널을 연 채 Ctrl+P → 미리보기 글자가 **안** 커졌는가 (D37-4)
5. 설정 [크게]/[아주 크게]에서도 표가 패널을 안 넘기는가
6. 다크 모드에서 [넓게] 버튼 대비 ([[project_soban29]] — 밝기 스캐너는 bg 축 전용)

---

## 6. 채택하지 않은 것

| 안 | 기각 사유 |
|---|---|
| 드래그 리사이즈 핸들 + 폭 기억 | 포인터 이벤트·최소/최대 클램프·터치까지 구현량이 가장 크다. 2단 토글로 실사용 대부분을 덮는다 |
| 전체화면(inset-4) 모드 | 뒤 본문이 완전히 가려져 1.4 본표와의 대조가 끊긴다 |
| 전역 기본 배율을 lg로 | 이 패널만의 문제를 전 화면 변경으로 푸는 것. 이미 사용자가 3단 조절기를 갖고 있다 |
| 토큰 실값(`--fs-1~6`) 상향 | 모든 배율 단계가 동시에 커지고 S0 기준선을 다시 떠야 한다. 인쇄 목록과도 한 쌍이라 표면이 넓다 |

## 7. 선택 후속 (미구현)

- **나란히 미리보기 켤 때 자동 [넓게]** — `plan-form14-specs.tsx`의 `toggleSplit`에서
  커스텀 이벤트를 쏘고 부모가 받는다(기존 `erp:open-spec-section` 버스와 같은 패턴).
- **미리보기 iframe 높이** — `plan-form14-specs.tsx:1033`의 `h-[640px]` → `h-[min(78vh,900px)]`.
