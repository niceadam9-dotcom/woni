# 소방계획서_10 — 입력 동선 통합: 모드·토글 폐기 "항상 편집" · 화면 확장 · 건축물대장 자동화 (2026-08-05)

## 1. 확정 원칙 (사용자 결정)

| # | 원칙 | 결정일 |
|---|---|---|
| D-1 | **건축물대장은 주소 확정 시 자동 반영** — 소방계획서 탭 [건축물대장 불러오기] 버튼을 누르지 않아도, 주소(bcode)가 확정되는 순간 대장 확장 필드가 자동으로 채워진다 | 2026-08-05 |
| D-2 | 자동 반영은 **'빈 칸만'** — 수동 입력값은 절대 덮어쓰지 않는다(건물·시설 '빈 칸만 채움' 철학과 일치). 강제 덮어쓰기는 기존 [건축물대장 불러오기] 버튼 유지 | 2026-08-05 |
| D-3 | 자동 트리거는 **① 건물·시설 탭 저장 직후 + ② 소방계획서 탭 진입 시** 두 지점 | 2026-08-05 |
| D-4 | **서식 전체 트리가 소방계획서 기본 화면** — 빠른입력/서식전체 모드 토글 폐기, 트리로 단일화 | 2026-08-05 |
| D-5 | ~~**⚡ 빠른 입력 = 트리 최상단 노드(랜딩 기본)**~~ — **D-10으로 폐기(2026-08-06)** | 2026-08-05 |
| D-6 | **소방계획서 탭만 전체 폭** — `max-w-3xl`(768px) 해제 + 우측 고객 요약 패널 접힘. 다른 탭은 기존 폭 유지 | 2026-08-05 |
| D-7 | **기본정보 탭 = 항상 편집 가능한 촘촘 그리드** — 요약/편집 모드·[편집]·[요약 보기] 버튼 폐기. 모든 필드를 2~3열 그리드로 항상 표시·수정 | 2026-08-05 |
| D-8 | **1.1 계획서 정보 패널 = 항상 편집** — 요약/편집 모드 토글·[편집] 버튼 + **아코디언(접기)까지 폐기**, 열자마자 편집 폼(①시설 ②운영 ③화재보험 + 자위소방대) 노출 | 2026-08-05 |
| D-9 | 공통 안전장치 보존 — 저장/취소는 값 변경 시에만 노출(isDirty), 점검계획일 변경 시 확정 일정 전파 팝업, 전체 검증, 주소 검색의 관할소방서·건물 주소·bcode 전파 | 2026-08-05 |
| D-10 | **⚡ 빠른 입력 페이지 폐기 — 소방계획서 탭 진입 = 1.1 일반현황 입력폼**(D-5 번복). 요약·체크리스트 화면을 한 단계 거치지 않고 바로 입력한다 | 2026-08-06 |
| D-11 | **1.1 저장 버튼 단일화** — 송달 동의를 1.1 폼 상태·서버 액션에 흡수해 화면의 저장 버튼을 1개로. 같은 `customers` 행을 갱신하므로 액션을 나눌 이유가 없다 | 2026-08-06 |

## 2. 현행 진단 — 왜 바꾸나

1. **수동 대장 불러오기의 마찰**: 주소는 건물·시설 탭에서 넣지만, 소방계획서용 확장 필드(건축허가일·건축면적·세대수·동수·승강기·주차장·구조·지붕·높이 등 `LEDGER_FIELDS`)는 소방계획서 탭에서 [건축물대장 불러오기]를 **따로 눌러야** 채워졌다. 주소를 넣었는데 값이 비어 보이는 혼동.
2. **모드 전환의 인지 비용**: 소방계획서(빠른입력↔서식전체), 기본정보(요약↔편집), 1.1 계획서 정보(요약↔편집+아코디언) — 화면마다 "지금 어느 모드지", "편집하려면 버튼을 눌러야 하나"를 계속 판단해야 했다. 인라인 편집이 들어오면서 [편집] 버튼의 고유 역할도 좁아진 상태.
3. **좁은 작업 폭**: 고객 상세 탭 전체가 `max-w-3xl`(768px)로 묶여 있고 우측 요약 패널이 폭을 더 뺏어, 서식 폼이 답답하게 갇혔다.
4. **발견성 저하**: "요약 값을 클릭하면 편집된다"를 모르는 사용자에겐 편집 진입 자체가 숨겨져 있었다.

→ 방향: **"모드를 없애고 항상 보이고 항상 편집" + 대장 자동화 + 화면 확장**. 소방계획서_8의 트리 단일 허브(D-4·D-12)를 입력 동선 전반으로 확장한다.

## 3. 변경 내역

### 3-1. 건축물대장 주소 확정 시 자동 반영 (D-1·2·3)
- **`autoApplyLedgerEmptyAction`** 신규 (`fire-plan-info-actions.ts`) — 활성 건물의 bcode·지번(없으면 저장된 주소로 Juso 지오코딩 후 백필, B안)으로 대장 조회 → `LEDGER_FIELDS` 중 **비어있는 칸만** 저장. 리다이렉트 없는 권한 체크(best-effort), 재조회 방지용 `ledger_synced_at` 항상 기록.
- **트리거 ①**: 건물·시설 탭 저장 직후 bcode가 있으면 자동 호출 (`building-inline-panel.tsx`).
- **트리거 ②**: 소방계획서 탭 진입 시 `ledgerAutoNeeded`(활성 건물이 미동기화이고 주소 보유)면 마운트 1회 자동 조회·반영 (`plan-tab-view.tsx` + `[id]/page.tsx`).
- 기존 [건축물대장 불러오기] 버튼은 **강제 재조회·덮어쓰기 미리보기** 용도로 유지.

### 3-2. 소방계획서 서식 전체 기본화 + 전체 폭 (D-4·5·6)
- `plan-tab-view.tsx` — `mode`(quick/full) 상태·토글 버튼 폐기. **서식 전체 트리가 항상 기본 화면**.
- 트리 최상단에 **`⚡ 빠른 입력` 노드**(랜딩 기본) 신설. 기존 빠른 입력 콘텐츠(필수 완성도·필요문서 칩·건축물대장 불러오기·누락 칩·**자체점검 송달 동의**·보관함 요약)를 `quickPanel`로 묶어 이 노드에 **한 페이지**로 표시.
- `customer-tabs.tsx` — `summary`·`fullWidthKeys` prop 추가. `fullWidthKeys=['plan']` 탭이 활성이면 `max-w-3xl` 해제 + 요약 패널 접힘. `[id]/page.tsx`는 요약 패널을 `summary` prop으로 이관.

### 3-3. 기본정보 탭 — 요약/편집 통합, 항상 편집 (D-7·9)
- `edit-customer-info-client.tsx` 재작성 — `mode`(summary/edit)·[편집]·[요약 보기] 버튼·요약 전용 인라인 퀵 입력(quick·saveQuick·openEdit) 폐기.
- **2~3열 촘촘 그리드**에 전 필드 상시 표시·수정: 점검유형/점검계획일*/고객명*/계약일/사용승인일/관할소방서/점검료(읽기)/주소(전체 폭)/비고(전체 폭).
- 보존: 저장/취소는 isDirty 시에만, 점검계획일 확정 일정 전파 팝업, 전체 검증, **주소 검색 = 선택 즉시 저장+전파**(`quickAddressApplyAction` — 관할소방서 자동 매핑·건물 주소·bcode). 도로명 수기 보정은 [저장]으로.
- 이로써 이전에 [편집] 버튼만이 하던 ①고객명 ②주소 세부 수기 ③일괄 수정이 전부 상시 편집으로 흡수됨.

### 3-4. 1.1 계획서 정보 패널 — 항상 편집, 아코디언 폐기 (D-8·9)
- `fire-plan-info-panel.tsx` 재작성 — `mode`(summary/edit)·`open`(아코디언) 상태 폐기, [편집]·[요약 보기] 버튼·요약 그리드 블록 삭제.
- 열자마자 **①시설현황 ②운영현황 ③화재보험 + 자위소방대 편집 폼**이 바로 노출.
- **헤더의 준비율 게이지 + 누락 칩은 유지** → 요약 그리드가 하던 "값 스캔·누락 확인" 역할을 대신(1.1은 이미 헤더에 준비율이 상시 표시돼 실손실 작음).
- 도구(추천값 채우기·다른 고객에서 복사)는 상단 툴바에 유지. `focusMissing`·추천/복사·save의 `setMode` 참조 정리(항상 편집이라 모드 전환 불필요).

## 4. 구현·검증 상태

| 항목 | 파일 | 커밋 | 검증 |
|---|---|---|---|
| 3-1 대장 자동 반영 | fire-plan-info-actions·building-inline-panel·plan-tab-view·page | `691ff36` | tsc·build 그린 |
| 3-2 서식 전체 기본화·전체 폭 | plan-tab-view·customer-tabs·page (+E2E 2건) | `c181284` | tsc·build 그린 |
| 3-3 기본정보 항상 편집 | edit-customer-info-client·page | 미커밋 | tsc·build·lint 그린 |
| 3-4 1.1 항상 편집·아코디언 폐기 | fire-plan-info-panel (+test-plan-tab) | 미커밋 | tsc·build·lint 그린 |

- **DB 마이그레이션 없음** — 3-1은 기존 `ledger_synced_at` 컬럼 재사용, 나머지는 UI 전용.
- **E2E 스크립트 갱신**: `test-plan-tab.mts`(토글 클릭→트리 노드, 1.1 [편집] 클릭 제거), `_h25-e2e.mjs`(빠른입력 복귀→⚡ 노드). `test-cd-ui.mts`의 `fp-receiver`·`cf-approval` 포커스는 필드가 상시 존재해 그대로 유효.
- **E2E 실행은 미완**(dev 서버 + 로그인 + 스테이징 DB 필요) — 로컬 실행으로 실동작 확인 권장.

### 3-5. 빠른 입력 페이지 폐기 — 랜딩 = 1.1 일반현황 (D-10, 2026-08-06)

**배경**: D-5로 랜딩이 된 ⚡ 빠른 입력(요약·체크리스트 한 페이지)을 하루 써 본 결과, 입력하러 들어올 때마다 요약 화면을 한 번 거쳐야 해 오히려 단계가 늘었다는 사용자 판단. **"탭 누르면 바로 일반현황 입력폼"**으로 확정.

**제거**: 온보딩 배너(고객 등록 완료)·필요 문서 칩·필수 완성도 카드(게이지+[건축물대장 불러오기]+대장 미리보기/확정저장)·지도·사진 링크·보관함 요약 → 전부 삭제. 트리의 `quick` 노드도 삭제.

**보존(이관·대체 경로 확보)**:
- **자체점검 송달 동의** — 시스템 내 **유일한 입력 화면**(별지 9호 1쪽 법정 항목, 타임라인이 "고객 소방계획서 탭에서 입력"으로 지목). 삭제 시 관계인 이메일 발송이 영구 차단되므로 **1.1 하단으로 이관**(`id="consent-section"` 유지, 누락 칩 라우팅도 `select('1.1')`로 변경).
- **누락 칩(입력처 이동)** — 완성도 카드가 사라지면 소방계획서_9 B안에서 배선한 건물 칩(`bf-*`) 라우팅이 사장되므로, **상단 생성 바의 누락 텍스트를 클릭 가능한 칩으로 승격**해 `gotoMissing` 경로 유지.
- **건축물대장** — 수동 미리보기·확정 저장은 폐기하되, ① 탭 진입 시 자동 반영(3-1) ② 1.1 [건축물대장에서 다시 가져오기] ③ 건물·시설 탭 수기 입력(소방계획서_9 B안) 3경로가 이미 있어 기능 손실 없음.
- **지도·사진·보관함** — 트리에 전용 노드가 있어 링크·요약만 제거.

**정리**: `docs`·`quick`·`latestPlan`·`onboardingSteps` prop 및 부모의 `requiredDocs`/`computeQuickReadiness` 산출 제거, `plan-onboarding-banner.tsx` 삭제(고아), 미사용 import 정리.

**검증**: tsc 클린 · eslint 기존 2건 외 신규 0 · `test-plan-tab` 41/1(잔여 1건은 설비 대장 슬라이드 패널 오버레이로 본 변경과 무관, 변경 전과 동일 지점) — 랜딩·폐기·송달 동의 이관·누락 칩 유지 신규 체크 8건 전부 통과.

### 3-6. 1.1 저장 단일화 + 화재보험 라벨 복구 (D-11, 2026-08-06)

**저장 두 곳 문제**: 3-5로 송달 동의가 1.1로 오면서 한 화면에 [저장]이 둘(계획서 정보 / 송달 동의)이 됐다. 두 값 모두 결국 같은 `customers` 행을 갱신하므로 액션을 분리할 이유가 없어 **통합**.
- `FirePlanInfoInput`에 `emailConsent`·`reportEmail` 추가, `saveFirePlanInfoAction`의 customers update에 2컬럼 합류. 이메일 형식·"동의 시 이메일 필수" 검증은 구 액션 규칙을 그대로 승계.
- `fire-plan-info-panel`에 **④ 자체점검 보고서 전자우편 송달 동의** 섹션 신설(`id="consent-section"` 유지 → 누락 칩 라우팅 무변), 앵커 바에 ④ 추가.
- `plan-tab-view`에서 consent state·`saveConsent`·전용 저장 버튼·`consentInitial` prop 제거. `saveEmailConsentAction`은 흡수되어 삭제.

**화재보험 라벨 결함**: ③ 화재보험 4칸만 `<label>` 없이 `placeholder`로 항목명을 표시해, **값을 넣는 순간 무슨 칸인지 사라졌다**(①②는 정상 — 국소 회귀). 더해 별지 9호 원문이 가입금액을 **천만원 단위**로 인쇄(`report9.ts` page2)하는데 화면에 단위 표시가 없어 자릿수 오입력을 유발했다.
- ①②와 동일한 `label` 부여(가입 여부·보험사·가입기간·대인/대물 가입금액) + 금액 칸에 **`천만원` 접미 상시 표시**, 예시 placeholder를 서식 기준으로 교체.

**검증**: tsc·eslint 그린(기존 ref 경고 2건 외 신규 0) · `test-plan-tab` **44/1** — 저장 통합·라벨·단위·통합 저장 DB 반영 신규 4건 포함 전부 통과, 잔여 1건은 설비 대장 슬라이드 패널 오버레이(본 변경 무관).

## 5. 잔여·후속

- E2E 실행(test-plan-tab·_h25-e2e·test-cd-ui) 및 로컬 화면 육안 검증.
- 3-3·3-4 커밋 + 두 커밋(691ff36·c181284 포함) push(진행 승인 시 pre-push 빌드 게이트).
- 후속 검토: 다른 탭(관계인·청구 등)에도 동일 "항상 편집" 통합을 적용할지 여부.

> 참고: `소방계획서_9.md`는 별개 주제(설비 대장 세부 제원 간편 입력·숫자 스테퍼·v2 확장)로 선점되어 있어 본 문서를 `_10`으로 채번함.
