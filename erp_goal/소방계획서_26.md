# 소방계획서_26 — 소방시설(1.4)에서 설비별 점검결과 ○×／ 직접 입력

작성: 2026-08-21. 요구: 소방계획서 > 소방시설에서 설비를 체크([√])하면 별지 4호 1쪽·9호 3쪽
결과칸에 반드시 ○/×/／가 찍혀야 한다 — 공란 재발방지. 기존 잘못된 데이터 정리는 범위 밖(사용자 지시).

## 용어

- **소방계획서 체크** = 서식 1.4 소방시설현황의 설비 [√] (`fire_facilities.installed`, 고객 단위 마스터)
- **점검표입력** = 점검표 시트 응답 (`inspection_sheet_responses`, 회차 단위 · 항목 단위 · O/X/N)
- 별지 3쪽 결과칸 = 점검표입력을 시트→설비로 롤업(`rollUpForm3Results`)한 파생값

## Q-1. "○ = 시트 미입력 일괄 ○" 재검토 (2026-08-21 사용자 요청)

쟁점: 설비 행에서 ○ 한 번 = 그 시트 항목 수십 개가 한꺼번에 ○ — **항목을 안 보고 ○를 깔 위험**.

| 안 | 내용 | 판정 |
|---|---|---|
| A | 일괄 ○ 유지(미입력만·확인창) | 선례 있음(`bulkAllGoodAction`, §9-4 A안 "① 전체 양호 → ② 불량만 태깅"). 그러나 항목이 안 보이는 채로 눌린다 |
| B | 항목 확인 강제(일괄 금지) | 정직하나 점검표 화면과 중복이고, 일괄이 없으면 이 기능의 가치(속도)가 없다 |
| C | 설비 단위 별도 저장소 | **기각** — 3쪽 ×인데 불량내역 0건(별지 10호 공백) 모순 문서가 생긴다 |

**결정: A+B 하이브리드.** 패널이 그 시트의 **항목 목록을 그대로 펼쳐 보여주고**(항목별 ○✕／ 버튼),
상단에 [미입력 전부 ○]·[미입력 전부 ／] 일괄 버튼을 둔다. 항목이 눈앞에 보이는 상태에서 일괄을
누르므로 "안 보고 ○"가 아니라 "보고 나서 한 번에 ○"가 된다. 일괄은 단축이고 항목별이 정본.

## 설계 결정

- **D-1 단일 원천**: 1.4의 설비별 입력은 별도 저장소가 아니라 **그 시트의 항목들을 쓰는 단축 입력기**다.
  문서·화면·점검표가 같은 데이터를 본다.
- **D-2 회차 축**: 1.4는 고객 마스터라 회차가 없다 → **진행 중(미완료) 최신 자체점검 회차**를 서버가
  해석(`getLatestSpecialInspection` + `requireActive`). 최신 회차가 completed면 null — 더 옛 회차로
  내려가지 않는다(끝난 회차에 쓰는 사고 방지). 없으면 입력 비활성 + 안내. 패널 머리에
  "○○년 N차 회차에 기록됩니다" 명시.
- **D-3 ×는 항목 지정 필수**: ✕ 저장 → `createDefectsFromXAction`(불량내역 자동 등록 — 별지 10호·⑤⑥단계의
  원천)까지 기존 체인 그대로. 설비 통째 X는 불량 수십 건 오염 또는 10호 모순이라 금지.
- **D-4 일괄은 미입력만**: 기존 ○/✕/／ 절대 무덮음(`bulkAllGood`·`bulkSheetNA` 규약 승계). 확인창에
  개수·형제 설비·회차 라벨 명시.
- **D-5 형제 고지**: 한 시트가 여러 설비를 덮는다(예: '유도등 및 유도표지' = 유도등·유도표지·피난유도선).
  패널이 형제 설비와 설치 상태를 함께 표시 — 형제 배지도 함께 바뀐다는 사실을 숨기지 않는다.
- **D-6 선행 결함 수정**: `SheetStat={any,x}` 두 축이라 **응답이 전부 ／여도 ○로 인쇄**되는 결함
  (재현 `scripts/_probe-na-only-becomes-good.mts`)을 먼저 고친다. ／ 입력 기능을 넣는데 이걸 안 고치면
  ／를 누른 화면·문서가 ○를 보여준다. `o`(양호 있음) 축을 **필수 필드**로 추가 — 구성 지점 누락은
  tsc가 잡는다(scripts/는 tsconfig exclude라 프로브만 수동 갱신).
- **D-7 재사용**: 시트별 통계는 기존 `getInspectionSheetOverviewAction`의 `SheetProgress.counts{O,X,N}`를
  그대로 쓴다(새 통계 액션 없음). 항목 로드는 `loadSheetEditorAction`, 저장은 `saveSheetResponsesAction`,
  ／ 일괄은 `bulkSheetNAAction`. 신규 액션은 2개뿐: 회차 해석·시트 ○ 일괄.

## 단계

- **S1** `sheet-facility-map.ts` — SheetStat 3축화(`{any,x,o}` o 필수) + `foldSheetResult` 헬퍼 +
  `rollUpForm3Results` 판정(X→× / O→○ / 전부N→／ / 무응답+미설치→／ / 무응답+설치→공란) +
  `axisWarnings.respondedNotInstalled`는 O·X 있는 시트만(전부-N은 '해당 없다'는 진술이지 대장 누락 근거가 아님).
  구성 지점 2곳(`report9-actions.ts` 별지 조립 · `facility-spec-actions.ts` 1.4 배지)을 foldSheetResult로 통일.
  프로브 7곳 갱신(test-form3-axis 케이스 추가 + 돌연변이 확인).
- **S2** `latest-inspection.ts` — `requireActive` 옵션(최신 1건이 completed면 null) +
  `facility-spec-actions.ts`에 `getActiveSpecialInspectionAction(customerId)` 신설.
- **S3** `sheet-actions.ts` — `bulkSheetGoodAction(inspectionId, sheetId, month=0)` 신설:
  `bulkSheetNAAction` apply 분기의 거울(미입력만 'O' insert · isItemInScope · syncInspectionSteps ·
  revalidate 2경로). release 없음 — 잘못 눌렀으면 항목 단위로 고친다.
- **S4** 1.4 UI (`plan-form14.tsx` + `customers/[id]/page.tsx` canRegister 배선):
  설치 행에 결과 배지(○ 녹/× 적/／ 회/미입력 호박) → 배지 클릭 시 표 아래 패널:
  회차 라벨 + 형제 고지 + [미입력 N개 전부 ○]/[전부 ／]/(N 있으면)[／ 해제] + **항목 목록**(항목별 ○✕／,
  ✕는 메모 입력 후 불량 자동 등록). 저장 후 overview 재조회로 배지 재수렴.
  ⚠ '점검표 입력' 문구 금지(test-annex-interaction 문자열 카운트 판정).

## F-1 해소 (2026-08-21 후속 — 마이그레이션 148)

시트가 없던 9종의 정체는 **준용 구조가 아니라 시딩 누락**이었다. STD 시트 코드 = 고시 설비번호인데
DB의 빈 번호(5·6·7·8·10·12·18·24·30)가 고시 별지4의 독립 점검표 자리와 일치했고, 시딩 원천이던
회사 XLS에 그 탭이 없었던 것(항목 잔존 0건 실측 — `_recon-f1-db-state.mts`).

- **148 편입 7종·302항목**: 물분무(6, 62항)·미분무(7, 61항)·포(8, 83항)·분말(12, 55항)·
  누전경보기(18, 9항)·제연설비(24, 17항 — ERP 어휘 거실제연설비)·연소방지(30, 15항).
  원천은 `erp_goal/_form/_별지4호_현행판_추출.txt` 축자(137과 같은 정본), ●=종합전용,
  3층 축(그룹·[소제목])까지 원문대로. 생성기 `_extract-annex4-missing-sheets.mts`
  (그룹별 #코드=#항목 자기검증), 대조 프로브 `_probe-148-vs-source.mts` **50/50**(DB 전 필드 축자).
- **잔존 2종 — 강화액·고체에어로졸**: 고시 별지4에 점검표가 없다(원문 등장 0회). 억지로 만들면
  법정 서식 위조라 제외 — 1.4 패널이 "시트가 카탈로그에 없어 기록할 수 없습니다"를 계속 안내한다.
- **조기진압(5)·할론(10)은 원문상 독립 시트지만 현행 묶음 매핑(스프링클러·할로겐 시트) 유지** —
  기존 응답 데이터 연속성. 분리하려면 기존 회차 응답의 재귀속이 필요해 별도 결정 사안.
- 매핑 7키 추가(`SHEET_FACILITY_MAP`) 하나로 1.4 배지·패널·[전체 양호]·별지 3쪽 롤업·인쇄 번들이
  전부 자동 반영. 라이브 E2E **20/20** — 미분무 패널이 열리고(작동 회차라 ●제외 34항 전개),
  고체에어로졸은 안내 유지, 커버리지 재실측 9종→2종.
- ⚠ 교훈: `unstable_cache`는 파일 캐시로 **dev 재기동을 살아남는다** — DB에 시트를 넣고도
  화면에 안 보이면 이 캐시부터. **위치 정정(2026-08-22 실측): dev 모드는 `.next/dev/cache/fetch-cache`다**
  — 종전 기록(`.next/cache/fetch-cache`)은 빈 곳이라 지워도 효과가 없었고 재기동 2회가 헛돌았다.
  재기동 경합은 `.next/dev/types` 생성물을 깨뜨려 tsc 가짜 85오류를 만든다(삭제 후 재생성).
- 적용: 스테이징·**운영 모두 완료**(2026-08-21, 사용자 직접 실행 — 운영 실측 `_probe-prod-148.mts` 8/8,
  7시트·302항목·● 수까지 스테이징과 동일). 재기동 시엔 서버 fetch-cache 확인만 남는다.

## F-1f 이중 귀속 분리 (2026-08-22 사용자 승인 — 마이그레이션 150)

"묶음을 유지하면 문항 정합은 어디에서 처리하나"라는 질문이 결정을 갈랐다. 실측
(`_recon-f1f-item-delta.mts`): 묶음 시트에 없는 전용 문항이 **조기진압 14/73·할론 16/53** —
묶음 유지로는 고시 문항의 19~30%를 물어볼 수 없고, 델타를 묶음 시트에 끼워 넣으면 3번·11번
시트의 축자성이 깨진다. 즉 묶음 유지 하에서는 문항 정합을 처리할 자리가 없다.

- **이중 귀속**: 전용 시트(STD-05·STD-10, 150 편입 126항목)를 신설하되, 재귀속(응답 이관)은
  하지 않는다. `SHEET_FACILITY_MAP`은 1:1로 분리하고 `LEGACY_ROLLUP_MAP`이 레거시 간선
  (스프링클러 시트→조기진압, 할로겐 시트→할론)을 **롤업·형제 고지에만** 합산한다.
  선택(`sheetMatchesFacilities` — 노출·[전체 양호]·인쇄 대상)은 전용 시트만 잡는다.
- 효과: 과거 회차(스테이징 서림사 2026 완료 회차, 할로겐 시트 응답 27건)의 별지 3쪽 할론 ○가
  **무퇴행**으로 유지되고, 신규 입력은 전용 시트로 수렴한다. 운영은 설치 0곳이라 무영향.
- 수정 파일은 `sheet-facility-map.ts` 하나 — 호출부 전수 확인 결과 선택 3곳(main만)과
  롤업·고지 2곳(legacy 합산)의 방향이 코드 수정 없이 갈린다. 검증: `_probe-form3-map` 7절
  24/24 + 돌연변이 양방향(간선 제거→무퇴행 3건 실패 / 선택 합침→분리 2건 실패).
- ⚠ **고시 5번 원문은 5-L-001을 두 번 부여한다**(제어반 직속 '겸용' + [감시제어반] 첫 항목 —
  hwp 추출본·XML 정본 양쪽 실측. 3번은 겸용=001·감시제어반=011~라 충돌 없음). item_code는
  응답의 키라 첫 등장(겸용)을 `5-L-000`으로 최소 보정 — 문구 축자 유지, 000→031 오름차순 유지,
  생성기·프로브가 중복 전제를 양방향 단언(원문 개정으로 중복이 사라지면 검사가 알린다).
- 잔여: 150 스테이징·운영 적용(승인 후) → `_probe-150-vs-source` 2부 → 라이브 E2E.
  코드가 150보다 먼저 뜨면 할론·조기진압 행 패널이 '시트 없음' 안내로 잠시 퇴행(배지는 무퇴행).

## 범위 밖

- 기존 44칸 공란·잘못 인쇄분 정리(사용자 지시: 테스트 데이터, 무시)
- ①단계 완료 판정 강화(공란이면 미완료) — 하지 않기로 확정(2026-08-21)
- ~~조기진압(5)·할론(10) 독립 시트 분리~~ → 2026-08-22 F-1f로 착수(위 절)
