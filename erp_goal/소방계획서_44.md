# 소방계획서_44 — 별지 9호 2쪽 3행(소방계획서·자체점검·교육훈련)의 입력 자리를 소방계획서로 이관

작성 2026-09-07. 추적 정본: `erp_goal/소방계획서_44.json`.
상태: **구현 완료(code_present) · 미커밋 — Q-1~Q-4는 2026-09-07 사용자 결정으로 닫혔다(D-1~D-4).**
잔여: 커밋 승인 · 독립 판정(구현자 ≠ 판정자) · 배포 시 갑지 육안 1회.

프로브: `_probe-44-annex-scope.mts`(착수 전 실측) · `_probe-44-prev-year-duty.mts`(22/0) ·
`_probe-44-e2e.mts`(11/0). 게이트 재실행: `test-preview-pane` 19/0 · `test-xlsx-anchors` 61/0 ·
`test-xlsx-inject` 189/0 · `test-annex-overrides` 57/57 · `tsc` 0.

---

## 1. 발단 (사용자 요구, 2026-09-07 · `erp_goal/image-79.png`·`image-80.png`)

> "소방계획서, 자체점검, 교육훈련 — 위치가 소방계획서 이동하도록 제안해줘."

- **image-79** = 갑지 엑셀 「정보」 시트(별지 제9호서식 **8쪽 중 제2쪽** 「특정소방대상물 정보」).
  가리킨 3행: `소방계획서` / `자체점검(전년도)` / `교육훈련`.
- **image-80** = 그 3행의 값을 **지금 사람이 확정하는 자리** — 점검 건의 「별지 9호 — 고유값·미리보기」 패널.
  8칸 중 **6칸이 그 3행 전용**이다(보고일·비고만 별지 9호 고유값).

요구를 한 문장으로: **원천이 소방계획서인 값을, 점검 건 패널이 아니라 소방계획서에서 확정하게 하라.**

---

## 2. 지금의 사슬 (실측)

### 2-1. 세 행은 한 조립기에서 나온다 — 산출물은 갈라지지 않는다

| 산출물 | 렌더 위치 | 원천 |
|---|---|---|
| PDF 별지 9호 2쪽 | `lib/doc-templates/report9.ts:332·336-337·340-341` | `assembleReport9()` |
| 갑지 엑셀 「정보」 시트 | 앵커 `firePlanLine`=`정보!B10` · `prevInspectLine`=`B11` · `trainingLine`=`B12` (`lib/xlsx-anchors.ts:164-166`), 주입 `lib/xlsx-workbook.ts:256-259` | **같은** `assembleReport9()` |

→ **이관 대상은 조립기 하나(`lib/report9-assemble.ts`)뿐**이다. 엑셀 쪽 추가 배선은 0.

### 2-2. 자동 판정 (`report9-assemble.ts:294-324`)

| 행 | 자동 원천 | 성격 |
|---|---|---|
| 소방계획서 작성 | `fire_plan_forms.sections` 키가 하나라도 있으면 「작성」 (`:296-298`) | 소방계획서 그 자체 |
| 소방계획서 보관 | 별도 원천 없음 — **작성 여부를 그대로 따름** (`report9.ts:333` `d.firePlanStored ?? d.hasFirePlan`) | 근거 없음 |
| 자체점검(전년도) 작동·종합 | 같은 고객의 전년도 `status='completed'` 점검 이력 (`:299-313`) | ERP 이력 |
| 교육·훈련(전년도) | **소방계획서 서식 1.11.4** 결과 기록부의 전년도 행 (`:321-324`, `lib/training-records.ts`) | 소방계획서 |

**자동 판정은 부정을 단정하지 않는다**(A9-6). 실적이 없으면 「실시」도 「미실시」도 아닌 **양쪽 공란**으로 인쇄된다.
image-79에서 자체점검·교육훈련 두 줄이 통째로 비어 있는 것이 정확히 이 상태다.

### 2-3. 수기 확정 (`report9-assemble.ts:601-627`)

`annex_inputs`(점검 건 단위, `unique(inspection_id, annex_no)`)의 `fields` JSON 6키가 자동 판정을 덮는다.
입력 화면 정의는 `components/inspections/annex-fields.tsx:45-86`.
**「미실시·미작성·미보관」 √는 오직 이 6칸으로만 나간다.**

---

## 3. 그 자리가 틀린 이유 (실측 4건)

**D-1 — 축이 어긋난다.** 세 행은 전부 **(고객, 연도) 사실**인데, 저장은 **점검 건 단위**다.
「2025년에 소방훈련을 안 했다」는 사실이 그 점검 건에 묶여 있어, 같은 고객의 다른 회차에는 없다.

**D-2 — 값에 연도가 새겨져 있지 않다.** `eduDone: '미실시'`는 *어느 해의* 미실시인지 데이터가 모른다.
「전년도」의 뜻은 읽는 시점(`insp.year - 1`)이 정한다.

**D-3 — 그래서 이어받기가 값을 한 해 밀어 넣는다.** 첫 작성이면 직전 회차 입력을 복사 제안하는데
(`annex-compose-panel.tsx:98-111`), 제외 대상은 **날짜류(`date`·`daterange`)뿐이라 6칸이 전부 복사된다**.
직전 회차 판정은 **연도를 보지 않는다** — 시작일 기준 바로 이전 회차다(`facility-spec-actions.ts:304-314`).
→ 2026년 회차에 확정한 「2025년 미실시」가 2027년 회차로 넘어가 **2026년 실적으로 인쇄**될 수 있다.
사용자는 버튼 한 번 누를 뿐이고, 화면 어디에도 그 값이 어느 해 것인지 적혀 있지 않다.

**D-4 — 안내 문구가 이미 다른 화면을 가리키고 있다.** 소방계획서 1.11.4 배지는
"별지 9호 작성 패널에서 실시·미실시를 확정하세요"라고 **사용자를 밖으로 내보낸다**(`plan-form111.tsx:134`).
원천(1.11.4 기록부)과 확정 창구(별지 9호 패널)가 갈라져 있다는 것을 코드가 스스로 말하고 있다.

### 법정 근거 — 셋은 원래 한 묶음이다

서식 9쪽 작성방법 8호(`report9.ts:685`):

> "소방계획서", "자체점검(전년도)", "교육훈련(전년도)"은 「화재의 예방 및 안전관리에 관한 법률」
> 제24조에 따른 **소방안전관리업무 실시사항**을 기입합니다.

세 행은 별지 9호의 값이 아니라 **소방안전관리업무 실시사항**이다. 그 업무를 적는 문서가 소방계획서이고,
소방계획서 서식 1.10의 제목이 그대로 **「소방안전관리자 자체점검 및 업무 수행」**이다.

---

## 4. 제안 — 확정 자리를 소방계획서 1.10으로 옮긴다

### B안 (권장) — 1.10에 「전년도 업무 실시사항」 한 블록 신설

소방계획서 탭 `?tab=plan&form=1.10`(`components/customers/plan-form110.tsx`), 1.10.1 연간 자체점검 계획 **바로 아래**에
앵커 `c-1.10-prev`로 블록 하나를 만든다. 상단 앵커 칩 줄(`plan-form110.tsx:118-119`)에 항목을 하나 더한다.

```
┌ 전년도(2025년) 업무 실시사항 — 별지 9호 2쪽·갑지 「정보」 시트에 그대로 인쇄됩니다 ┐
│ 소방계획서   ○작성 ○미작성   (작성 시) ○보관 ○미보관       [자동: 작성 — 서식 입력 있음] │
│ 자체점검     작동 ○실시 ○미실시 · 종합 ○실시 ○미실시        [자동: 2025년 완료 이력 0건] │
│ 교육·훈련    교육 ○실시 ○미실시 · 훈련 ○실시 ○미실시    [자동: 1.11.4 전년도 실적 → 1.11.4로 이동] │
│ ※ 고르지 않으면 자동 판정대로, 자동 판정이 없으면 양쪽 공란으로 인쇄됩니다                  │
└──────────────────────────────────────────────┘
```

- 각 줄에 **자동 판정 결과를 그대로 보여 준다**(현재 별지 9호 패널의 `auto` 표시와 같은 규약).
  사람은 "자동과 다를 때만" 고른다 — 지금 패널의 3상태 규약(`''`/`실시`/`미실시`)을 그대로 옮긴다.
- 셋을 한 블록에 두는 근거는 §3 법정 작성방법 8호다. 흩어 놓으면 사용자가 세 화면을 돌아야 한다.

**교육·훈련만 예외 처리**: 실적의 원천은 1.11.4 기록부다. 여기서 「실시」로 확정하는 것은 원천을 비운 채
결과만 찍는 것이라 1.11.4 배지와 어긋날 수 있다. → 이 줄에는 **「1.11.4 기록부로 이동」 링크를 함께** 둔다(Q-2).

### A안 — 원천 3곳에 분산

소방계획서 작성·보관 → 「조회·개정이력」 노드 / 자체점검 → 1.10 / 교육·훈련 → 1.11.4 배지 아래.
원천 근접성은 최고지만 **한 번의 확정에 세 화면**이 필요하다. 법정 작성방법이 셋을 묶은 것과도 어긋난다.

### C안 (최소) — 패널은 그대로 두고 딥링크만 추가

별지 9호 패널에서 1.10·1.11.4로 가는 링크만 만든다. D-1~D-3(축·연도·이어받기)은 **하나도 해결되지 않는다.**

---

## 5. 저장 축 — 값에 연도를 새긴다

`fire_plan_forms.sections`에 새 키 `annexStatus` 하나. **DB 마이그레이션 없음**(JSONB).
`fire-plan-form-actions.ts:48-57`의 `FORM_SECTION_KEYS` 화이트리스트에 `'annexStatus'` 추가만 필요하다
(빠지면 `:65`가 저장을 거절한다).

```jsonc
"annexStatus": {
  // 키 = 실적 연도(= 점검연도 - 1). 2026년 회차는 "2025"만 읽는다.
  "prevYear": { "2025": { "edu": "실시", "drill": "미실시", "op": "", "comp": "실시" } },
  // 작성·보관은 '현재 상태'라 연도축 없음
  "plan": { "written": "", "stored": "미보관" }
}
```

**연도 키가 D-2·D-3을 구조로 없앤다.** 2027년 회차는 `"2026"`을 찾고, 없으면 자동 판정으로 돌아간다 —
작년 확정이 올해로 새는 경로가 데이터 모양에서 사라진다. 이어받기 버튼이 손댈 대상도 아니다.

### 우선순위 사슬 (`report9-assemble.ts:601-627` 개정)

```
① sections.annexStatus (소방계획서 확정)        ← 새 정본
② annex_inputs.fields  (레거시, 읽기만 유지)     ← Q-3
③ 자동 판정 (부정 단정 없음 — 종전 그대로)
```

---

## 6. 이관 비용 — 실측 0

`scripts/_probe-44-annex-scope.mts` (스테이징 실DB, 2026-09-07):

```
[1] annex_inputs(report9) rows = 1
    eduDone=0  drillDone=0  prevOpDone=0  prevCompDone=0  firePlanWritten=0  firePlanStored=0
    rows with at least one of the 6 = 0
[2] inspections rows = 175 / distinct (customer,year) = 174
    (customer,year) with >=2 inspections = 1   (max 2)
[3] groups where >=1 inspection has a confirmed value = 0 / conflicting key instances = 0
```

- **6칸에 값이 든 행이 0건이다.** 데이터 이관·충돌 해소 절차가 통째로 필요 없다 — **지금이 옮길 적기**다.
  (운영 DB는 업무데이터 전량 삭제 상태라 더 적다 — [[project_prod_data_purge_2026_08_24]].)
- 다만 **D-1의 "같은 사실 두 번 입력"은 현재 데이터에선 174분의 1**이다. 이관의 주된 근거는
  중복 입력이 아니라 **D-2·D-3(연도축 부재로 값이 조용히 밀리는 것)과 원천 근접성**이다 — 과장하지 않는다.

---

## 7. 손대는 곳 (B안 기준)

| # | 파일 | 변경 |
|---|---|---|
| 1 | `components/customers/plan-form110.tsx` | 「전년도 업무 실시사항」 블록 + 앵커 칩 1개. 자동 판정값을 서버에서 받아 표시 |
| 2 | `app/(dashboard)/customers/fire-plan-form-actions.ts:48-57` | `FORM_SECTION_KEYS`에 `'annexStatus'` |
| 3 | `lib/report9-assemble.ts:601-627` | 오버레이 사슬 ①②③ (§5) |
| 4 | `components/inspections/annex-fields.tsx:45-86` | `report9`에서 6칸 제거 — 보고일·비고만 남김 |
| 5 | `components/inspections/annex-compose-panel.tsx` | ① 1단에 **읽기 전용 3줄 요약 + 「1.10에서 확정」 딥링크** ② 이어받기 대상은 `FIELD_DEFS` 순회라 **4로 자동 소멸(§3 D-3 해소)** |
| 6 | `components/inspections/inspection-workbench.tsx` | **작업대 인라인에도 같은 안내 한 줄**(F-11 — 표면이 둘이었다) |
| 7 | `components/customers/plan-form111.tsx` | 배지 문구 "별지 9호 작성 패널에서" → 1.10 블록 링크로 |
| 8 | `lib/prev-year-duty.ts` (신규) | 자동 판정 + 확정 해석 + 요약 3줄의 **단일 원천** |
| 9 | `lib/doc-templates/report9.ts` · `lib/xlsx-workbook.ts` | D-4 — 「보관」 폴백 제거(두 산출물 동시) |

딥링크는 기존 규약을 그대로 쓴다: `/customers/{id}?tab=plan&form=1.10&from=report9&insp={inspectionId}`
(`annex-compose-panel.tsx:20-31`의 `ledger` 형태와 동일).

---

## 8. 관측 — 확인이 필요한 별건

**O-1.** image-79에서 「자체점검(전년도)」·「교육훈련」·「화재보험」 세 줄만 **빨간 글씨**이고
「소방계획서」 줄은 검정이다. 값 주입은 문자열만 바꾸고 셀 스타일은 건드리지 않으므로
(`xlsx-workbook.ts:256-259`) **템플릿 글자색 축**으로 보인다. 44의 범위는 아니지만,
제출본이 그 줄만 빨갛게 인쇄된다면 별건으로 잡아야 한다 — **실 생성물로 확인 후에만 결함으로 말할 것.**

---

## 9. 결정 (2026-09-07 확정)

| | 결정 |
|---|---|
| D-1 (Q-1) | **B안** — 1.10에 한 블록, 별지 9호 6칸은 걷는다 |
| D-2 (Q-2) | 교육·훈련 줄은 1.10에 두되 **1.11.4 링크 병기** |
| D-3 (Q-3) | 레거시 `annex_inputs` 6키는 **읽기 폴백으로만** 존치, 입력구 제거 |
| D-4 (Q-4) | 「보관」은 **사람이 고를 때만 √** — 미선택 시 보관·미보관 둘 다 공란(작성=보관 폴백 폐지) |

## 9-2. 구현 중 드러난 것 (설계가 못 본 것 2건)

**F-11 — ③계층 표면이 둘이었다.** `/inspections/{id}`에서 열리는 것은 슬라이드 작성 패널이 아니라
**작업대 인라인**(`inspection-workbench.tsx:1154` `AnnexFields`, 소방계획서_21 R6-6)이다.
정의(`FIELD_DEFS`)는 공유라 6칸 제거는 양쪽에 한 번에 먹었지만, **"어디로 갔는지" 안내는 한쪽에만
넣어 놨었다** — E2E 1차가 ④에서 타임아웃 나며 잡았다. 작업대에는 한 줄(연도·확정 여부·1.10 링크),
패널에는 3줄 요약. 세로가 귀한 자리(옆이 미리보기)와 아닌 자리를 구분한 것이다.

**F-12 — `annexStatus`가 자기 자신을 「작성」 근거로 삼을 뻔했다.** 작성 판정이 `sections` 키 개수라,
이 블록만 저장해도 「소방계획서 작성 √」가 켜진다. `judgePrevYearDutyAuto`에서 그 키를 판정 분모에서
제외하고 양방향으로 단언했다(B1·B2).

## 9-3. 대조군 — 무엇이 실제로 달라졌나

- **Q-4**: 실DB 표본 5건 중 `hasFirePlan=true`인 건에서, 종전 식 `firePlanStored ?? hasFirePlan`은
  `[√]보관`을 찍었다. 지금은 `[  ]보관 [  ]미보관`이다(`_probe-44-prev-year-duty` C).
- **F-5**: 다른 해 확정이 올해 칸에 적용되지 않는다(A4). 6칸이 `FIELD_DEFS`에서 빠져 **이어받기 대상
  자체가 사라졌다**(⑤).
- **저장 위치**: 확정이 `annex_inputs`가 아니라 `fire_plan_forms.sections.annexStatus`의 **연도 키**에
  남는다(E2E ②, 점검 건에는 안 쓴다까지 함께 단언).

---

## 10. 범위 밖

- 자동 판정 규칙 자체(전년도 이력 해석·`trainingDoneIn`)는 **손대지 않는다**. 옮기는 것은 확정 창구뿐이다.
- 별지 10·11호, 공문, 위임장의 ③계층은 그대로다(그 값들은 실제로 그 서식 고유값이다).
- O-1 글자색은 별건.
