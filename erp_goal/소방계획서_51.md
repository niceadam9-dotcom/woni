# 소방계획서_51 — 고객관리 3분리(공통·보고서·회차·소방계획서) 2단계: 1.1 이사·트리화·전년도 카드 이사

> 작성 2026-09-20 심야. **미완 인수인계 문서** — 토큰 소진으로 중단, 내일 이어서.
> 작업 트리: `F:\AI\ERP-tabs`(워크트리, 브랜치 `wip-fac-tabs`) — **아래 §3 변경분은 전부 미커밋 상태로 이 워크트리에만 있다.**

## §0 사용자 확정 사항 (이 순서로 확정됨 — 전부 유효)

1. **3분리 원칙**: 소방계획서를 「공통(별지·소방계획서 양쪽에 입력) / 보고서(별지에만 입력) / 소방계획서(계획서에만 입력)」로 구분.
   **이유: 소방계획서가 필요 없는 고객이 있다** — 공통+보고서만 채우면 별지 업무가 끝나야 한다.
2. 탭 순서: **공통 → 보고서 → 회차 → 소방계획서** (회차 = 구 별지서식, 회차 문서·별지 생성·점검표 진입 — 사용자: "별지서식 이전에 회차 관련 정보들").
3. **형식은 소방계획서 트리 구조와 동일하게** (좌측 목차·완성도 점·같은 조작감) — 공통·보고서 탭 둘 다.
4. **사용처 표시**: 각 입력 서식에 「별지 9호 2쪽」처럼 어느 문서에 실리는지 칩으로 표시.
5. 분류 재검토 결과(실측 기반, 사용자 승인):
   - 1.1 일반현황 → **공통** (화재보험·구조·수신기 등이 별지 9호 1~2쪽에도 인쇄)
   - 1.4 소방시설 → **공통** (1단계에서 이미 이사)
   - 「전년도 업무 실시사항」(sections.**annexStatus** — ⚠ dutyLog 아님!) → **보고서** (별지 9호 2쪽에만 인쇄, 계획서 워크북 50시트에 없음)
   - 기타 7종 분할·1.5·1.6·나머지 = 1단계 그대로.

## §1 이미 운영까지 나간 것 (1단계 — 완결)

- `4d4105b1`+`f2e77e59`+`ff9ef29b` 푸시, **운영 78회차 배포 완결**(가드·마커 3분법 전건 초록: 신규A 기타점검대상 0→1·신규B 공통안내줄 0→1·역방향 tab=plan&form=1.4 10→0·존속 3·2·1·음성 0·rollback-ff9ef29b·login 200, `_deploy78-up.sh`=기록 커밋 `8ea285d7`).
- 그 직후 **타 세션이 79회차**(`840dabd4` 별지 10·11호 이상없음 행 이행기간 제외) 배포 — 운영 HEAD는 그쪽이 최신.
- **:3000 공유 트리 = origin 동기화 완료**(리셋: 타 세션 WIP는 `F:\AI\ERP\_sync-backup-0920-tabs.patch` + 브랜치 `_backup-c368905d`(origin에 없는 별지10호 후속 커밋 — patch-id 부재 확인)로 보존). :3000 dev 재기동 상태.
- 별건 수리: :3000 Jest 워커 크래시 = `.next` 캐시 부패 → 삭제·재기동으로 해소.

## §2 지금 워크트리에 있는 것 (2단계 — 구현 완료·검증 일부)

**tsc 0 · 대장 검사(test-fire-plan-sections) 73/0 통과. E2E는 미실행** — 아래 §4가 남은 일.

| 파일 | 내용 |
|---|---|
| `lib/fire-plan-sections.ts` | '1.1'에 `tab: 'facilities'` 이사 표식(1.4와 같은 규약 — 대장에서 빼지 않음). `tabOfForm(form: string)`으로 완화(서버 딥링크 변환용) |
| `components/customers/tab-form-tree.tsx` **신설** | 공통·보고서 탭 공용 트리 셸 — plan-tab-view 트리 규약 축소 이식(?form= replaceState·미저장 확인·완성도 점·data-plan-node) + **usage(사용처 칩)** |
| `components/customers/plan-annex-status-card.tsx` **신설** | 「전년도 업무 실시사항」 독립 카드 — `{annexStatus}`만 부분 저장, getPrevYearDutyAction 자동판정, mark2 규약 그대로, 자체 [저장] |
| `plan-form110.tsx` | annexStatus 카드·상태·저장 키 제거(카드 3개로), 앵커 바에서 제외 |
| `[id]/page.tsx` | 딥링크 변환 일반화(`movedTab = tabOfForm(initialForm)` — 손목록 소멸), facilities 탭=TabFormTree(1.1+1.4, readiness 게이지·usage 칩), **reports 탭 신설**(etc 3종+duty, lazy·fullWidth), annex 라벨 「회차」, plan 뱃지 분모 12·warn=formFilled 축, 헤더 버튼 「회차」, 건물 탭 안내 `?tab=facilities&form=1.4` |
| `plan-tab-view.tsx` | 1.1 노드·form11 prop 제거, **랜딩 = CH1_FORMS[0](1.2)**, 임포트 배너를 트리 위 상단으로, 누락 칩 form11/consent → `gotoForm11()`(goTab facilities + focus-missing 재시도), 카드 앵커 구제 = **전체 이동** `?tab=<탭>&form=<노드>#해시` (goTab만으론 트리 기본 노드에 카드가 hidden) |
| `customer-tabs.tsx` | 미저장 가드 특례에 'reports' 추가 |
| 링크 정본화 | fsm-panel 1.1→`?tab=facilities&form=1.1` · annex-compose duty→`?tab=reports&form=duty&from=report9`·ledger→`…&form=1.4`·문구 「공통 탭 1.4」「보고서 탭 전년도…」 · plan-form14 fromParam 기본 `?tab=facilities&form=1.4` · plan-annex-sheet-tree 동일 · 바깥 라벨(작업대 「회차 문서」·타임라인 「회차 탭에서 보기」·달력/원장/목록 title 「회차…」) |
| 검사 갱신됨(미실행 포함) | test-fire-plan-sections([2b] 1.1·1.4 둘, STATUS 12·TREE 13 — **실행 73/0**), _probe-annex-tab(4탭 순서·회차 라벨·form=1.1 변환 — 미실행), test-plan-tab(랜딩 1.2·1.1 블록들 공통 탭 재조준·sub=ch1→1.2 — 미실행) |

핵심 함정 메모:
- ⚠ **TabFormTree는 패널을 hidden으로 전부 렌더**(입력 상태 유지 계약) — 딥링크·복귀 링크는 반드시 `&form=<노드>`까지 실어야 화면에 보인다. 이걸 빼먹으면 「탭은 맞는데 빈 것처럼 보이는」 부류.
- ⚠ annexStatus의 로드 전(null) 저장 금지 규약은 카드가 그대로 승계(빈 값으로 기존 확정 덮어쓰기 방지).
- ⚠ data-plan-node 표식이 이제 공통·보고서 트리에도 있다 — plan 트리 부재 단언은 `[role=tabpanel]:not([hidden]) aside` 스코프로(프로브에 반영함).

## §2.5 진행 기록 (이어서 세션)

- ✅ §3-1 E2E 배터리 **전건 초록**: 대장 73/0 · _probe-annex-tab 22/0(4탭 순서·회차·form=1.1 변환) ·
  test-plan-tab 106/0 · test-s7 16/0 · _probe-form14-etc 34/0(reports 탭 재조준) ·
  test-specs-panel-back 13/0 · test-annex-interaction 37/0 · test-allpass 23/0 · tsc 0
- 검사 수리 내역: s7 goto 4곳 &form=1.4 · form14-etc ⓪①⑤ 재조준 · allpass 라벨 「회차 문서」 ·
  plan-tab(랜딩 1.2 마커=「1.2.2 화재취약장소」(현황 없음)·1.1 블록 후 plan 복귀 goto 2곳·
  sub=ch1 goto ERR_ABORTED 1회 재시도·4.7은 공통 탭 클릭 후 [data-plan-node="1.4"] 노드 클릭)
- 제품 수리 1건: reports 탭 기타 카드 linkFrom → ?tab=reports&form=etc (트리 노드까지)
- ✅ §3-2 변이: M-a(1.1 tab 표식 제거 → 대장검사 5빨강+tsc 2에러) · M-b(movedTab 변환 제거 → 프로브 ② 4빨강) — 원복 후 전건 초록 재확인
- ✅ §3-3 커밋·푸시 완결: **`fc802dbb`** (origin/main). 리베이스 중 발견 — **타 세션이 80회차(`db2bbd9a`)를 이미 배포**, 내 배포는 **81회차**다.
- ✅ :3000 공유 트리 `git pull --ff-only` → fc802dbb (2단계가 :3000에서 보임)
- 🚨 **후속 발견(0921): fc802dbb가 링크 정본화에서 3표면을 놓쳤다** — 전년도 확정 카드는 보고서 탭으로
  갔는데 ①작업대 「1.10에서 확정」 링크(`?tab=plan&form=1.10&from=report9`)+「소방계획서 1.10에서
  확정됨」 문구 ②작성 패널 「소방계획서 1.10에서 확정됨」 문구 ③report9-assemble 미비 안내문 3건이
  여전히 1.10을 가리켰다(F-8과 같은 「죽은 안내」 부류 — 링크는 카드 없는 1.10에 착지). §3-4 역방향
  마커 계획(그 리터럴 양수→0)이 정확히 이 자리를 전제하고 있었는데 커밋이 빠뜨린 것.
  → 3표면+인접 주석 6파일 수리, 목적지 `?tab=reports&form=duty&from=report9`(annex-compose-panel:30과
  같은 정본). tsc 0.
- ✅ ③(선택) 전년도 카드 DB 왕복 E2E: **_probe-44-e2e.mts 재조준이 겸함** — ①③ goto
  `?tab=reports&form=duty`(hidden 전체 렌더라 &form= 필수)·저장 `annex-status-save`·④ 새 문구/딥링크
  단언+「낡은 1.10 자리를 더는 가리키지 않는다」 음성 단언. **:3211 실측 14/0 전건 초록**
  (확정→저장→DB `sections.annexStatus`→재로드 aria-pressed 복원·작업대 반영까지 한 사슬).
- ✅ 후속 정본화 커밋·푸시: **`d10089fd`**(8파일 — pre-push 게이트 전체 초록·origin 이동 0).
- ✅ **운영 81회차 배포 완결**(db2bbd9a→d10089fd·이미지 `bcdf849ce3d5`·rollback-d10089fd):
  신규A '별지 전용 입력 —' 0→1 · 신규B '보고서 탭에서 확정' 0→5 · **역방향
  'tab=plan&form=1.10&from=report9' 5→0** · 존속 3·2·3·1·2 유지 · 음성 0 · login 200.
  ⚠ 원래 신규B 후보 「회차 탭에서 보기」는 **프로덕션 청크가 문자열을 보존하지 않아 부적격**
  (로컬 .next server/static 실측 0 — dev/ 산출물만 잡혀 오검출 위험. 로컬 선실측이 잡았다).
  🚨 배포 ssh가 도구 타임아웃(120s)으로 끊겨 출력 유실 — 서버 프로세스는 끝까지 돌았고(롤백 태그
  존재가 증거) after는 재실측으로 판정. :3000 pull → d10089fd 완료.
- **잔여: 메모리 완결 갱신 + 81회차 기록 커밋**

## §3 내일 이어서 할 일 (순서대로)

1. **dev :3211 기동 후 E2E 배터리**: `TEST_BASE_URL=http://localhost:3211 npx tsx scripts/<t>.mts`
   - `_probe-annex-tab`(수정본 첫 실행 — 4탭 순서·회차·form=1.1) → `test-plan-tab`(수정본 첫 실행)
   - **`test-s7-multi-use-move` 수정 필요**: goto 3곳 `?tab=facilities` → `?tab=facilities&form=1.4` (트리 기본 노드가 1.1이라 카드 hidden)
   - **`_probe-form14-etc` 수정 필요**: ⓪ goto에 `&form=1.4` 추가 + **① 기타 3종 카드 위치가 annex → reports 탭으로 바뀜**(goto `?tab=reports&form=etc`, 복귀 from 단언도 tab=reports) + ⑥ 1.6 갈래는 불변
   - `test-specs-panel-back`(FORM14=`?tab=plan&form=1.4` 서버 변환이 form을 실어 1.4 노드 열림 — 그대로 초록 예상) · `test-annex-interaction`(회차 라벨 — [9호로 돌아가기] 도착 마커 동일·ledgerHref includes tab=facilities 그대로) · **`test-allpass-step-collapse` 수정 필요**: 작업대 링크 라벨 「보고서」→「회차 문서」(`a[href*="tab=annex"]:has-text("회차 문서")`)
   - 전년도 카드 신규 검증(권장): reports 탭에서 칩 확정→저장→DB `sections.annexStatus` 반영 + 1.10 저장이 annexStatus 안 건드림(부분 저장 무손상 — s7 5)와 같은 축)
2. **변이 최소 2종**: (M-a) fire-plan-sections '1.1' tab 표식 제거 → 대장검사+tsc 빨강 (M-b) page.tsx movedTab 변환 제거 → _probe-annex-tab ② 빨강
3. 커밋(1커밋: 3분리 2단계) → origin 이동 재측정·리베이스 시 재검증 → 푸시(pre-push 훅은 **:3211 dev 끄고** — 같은 .next 경쟁으로 훅 빌드가 거짓 실패한 전례)
4. **운영 80회차 배포**(_deploy78-up.sh 본떠 — 마커 후보: 신규 '전년도 업무 실시사항 저장됨'/'공통 서식이 늘면'은 78 기존이라 부적격 → 신규A '별지 전용 입력 —'(reports 안내줄)·신규B 'usage-duty' 또는 '회차 탭에서 보기' / 역방향 `label: '보고서'`는 문자열 부적격 → `tab=plan&form=1.10&from=report9` 리터럴 양수→0이 깔끔) + :3000 `git pull --ff-only`
5. 메모리(project_customer_tabs_reorg.md) 완결 갱신

## §4 환경 메모

- 워크트리 dev: `cd F:\AI\ERP-tabs\erp && npx next dev -p 3211` (node_modules 실설치 — 정션 아님·npm ci 가능). :3000은 공유 트리(F:\AI\ERP, origin 동기화됨).
- 운영: `root@1.201.116.205`(`~/.ssh/sjfire-erp-key.pem`), repo `/home/ubuntu/woni`, 가드/마커 절차는 `_deploy77-up.sh`·`_deploy78-up.sh` 규약.
- 회차·구간·HEAD는 반드시 서버 실측에서(동시 세션이 79회차를 그새 배포한 전례 — 오늘만 origin이 4번 움직임).
