# 소방계획서_39 — 설치 설비 점검표 필수 입력 강제(작동·종합 공통) + ● 종합 필수 축 + 미입력 설비 해소 유도

작성 2026-09-02. 추적 정본: `erp_goal/소방계획서_39.json`.
**상태: 착수** — 구현자는 `code_present`까지만 올린다(Json_Rule 규칙 3).

## 0. 결정 변경 (2026-09-02, 착수 중 사용자 확대 지시)

착수 시점 범위는 "**종합 회차의 ● 항목** 필수"였다. 구현 중 사용자 확정:
> "작동 또한 종합 동일하게 — 설비시설 체크하면 반드시 점검표 입력 되도록"

**최종 축**: 설치(1.4 대장 체크)된 설비 시트는 **회차 종류 무관** 범위 내(in-scope) 전 항목을
○/×/／ 중 하나로 기재해야 한다(빈칸 금지). 세부:
- 작동 회차: ●는 범위 밖(자동 ／, `5389f6d`) → 필수 대상 = ○ 항목 전부
- 종합 회차: 필수 대상 = ●+○ 전부. **●는 법정 근거가 명시된 부분집합**이라 배지·문서 고지에서 따로 센다
- 강제 분모는 `설치 시트의 (total − responded)` 합 — sheet-overview가 이미 주는 값이라 서버 필드 추가 불요.
  ●만의 수(compBlank)는 배지·고지용으로 병행 유지.

## 1. 발단 (사용자 요구, 2026-09-02)

- `image-43.png`: 현황(별지 9호 3쪽)에서 "체크(√)된 설비는 반드시 ○/×" → 같은 날 **기본 ○ 정책**(`0aceb39`)과 **발행 가드 팝업**(`efc3c66`)으로 처리.
- `image-48.png`: 점검표(스프링클러) — ●(종합 전용) 항목이 빈칸인 채 산출물에 나감. 사용자: "종합의 경우 ●는 반드시 점검해야 하는 것 — ERP가 반드시 입력하게 만들어라. 1~3층 전부 진행."
- 추가 확정: "설비시설을 체크하고 점검표 입력을 안 하면 **체크를 취소하도록 유도** — 시스템상 엑셀·PDF에 잘못된 빈칸이 안 나가게 반드시."

## 2. 법 근거 (원문 실측 — 요약 금지 규약에 따라 위치만)

| 층 | 원천 | 내용 |
|---|---|---|
| 법률 | 소방시설 설치 및 관리에 관한 법률 제22조 | 자체점검 의무. 미실시 1년 이하 징역/1천만원 이하 벌금 |
| 시행규칙 | 별표 3 | 종합점검 = **작동점검을 포함하여** … **소방청장이 고시하는 종합점검표에 따라** 점검 |
| 고시 | 별지 4호 각주 (`_form/_별지4호_현행판_추출.txt:508`) | 「※ 점검항목 중 "●"는 **종합점검의 경우에만 해당**한다.」 |
| 고시 | 별지 4호 1쪽 범례 | 「점검결과란은 양호○, 불량X, **해당없는 항목은 /표시**를 한다.」 |

**⚠ 핵심 뉘앙스**: 강제 대상은 "○/×"가 아니라 **"○/×/／ 중 하나"**다. 종합이라도 그 대상물에
구성이 없는 ● 항목(예: "다른 소화설비와 **겸용 시**…")은 ／가 법정 표기. 서식이 예정하지 않은
유일한 상태는 **빈칸**이다. 확인 2026-09-02 (법제처 별표3 + 생활법령 대조).

## 3. 축 관계 — 이 문서가 대체하지 않는 것

| 축 | 주관 | 39의 관계 |
|---|---|---|
| 작동 회차 ● 자동 ／ (문서·입력 화면 3면) | 타 세션 `5389f6d` (2026-09-02) | **대칭의 반대편**. 39는 종합 회차만 만진다. `outOfScope` 렌더는 소비만 |
| 설치+무응답 → 현황 요약 기본 ○ | `0aceb39` (같은 날) | 39는 **항목 층**. 항목 단위는 기본값을 만들지 않고 입력을 강제한다(항목 위조 방지) |
| 발행 가드 팝업 (설비 단위) | `efc3c66` (같은 날) | 39 S2가 **분모를 확장**(설비 단위 + ● 항목 단위 병기) |
| `sheet-item-editor.tsx` 크기 축(38 S5)·색 축(36 S6) | 38·36 | 39는 **내용 축**(● 배지)만. 축 소유 주석 유지 |

신규 축이므로 `supersedes` 없음, `related`로만 연결.

## 4. 설계

### S1 — 1층: 보이게 (표시)

- **S1-1** `sheet-item-editor.tsx` ItemRow: `comprehensive_only && !outOfScope`(= 종합 계열 회차의 ● 항목)이면
  항목명 앞 `●` + 미입력 시 amber 강조 + title(법 근거 문구). 두 입력 표면(전용 페이지·드로어)이
  편집기를 공유하므로 배선 1곳.
- **S1-2** 드로어(`inspection-sheet-client.tsx`) 헤더에 `● 미입력 N` 카운터(selCounts에 compBlank).
  overlaidProgress 오버레이도 compBlank 재계산(스프레드만 하면 저장 직후 낡음).
- **S1-3** 전용 페이지(`sheet-entry-client.tsx`) 헤더에 `● 필수 미입력 M건` — 기존 `미입력 N개`
  문구는 test-sheet-entry-page 정규식이 보므로 **보존하고 병기**. patchLocal이 compBlank 재계산.

### S2 — 2층: 나갈 때 잡기 (소프트 가드) + 설비 해소 유도

§0 확대에 따라 가드 분모는 **필수 미입력 = 설치 시트의 범위 내 무응답 항목 합**(작동·종합 공통).

- **S2-1** 전용 페이지 backHref 이탈 시 confirm(필수 미입력>0). OK=이탈 진행(비차단).
- **S2-2** 드로어 requestClose 선두 confirm — ESC·백드롭·✕·[닫기] 4경로가 이 한 점을 지남(실측).
- **S2-3** 회차 카드 발행 가드(`blanksGuardThenGo`) 분모 확장: 설비 단위 미입력 + 필수 미입력 항목 병기(종합이면 ● 수 포함 표기).
- **S2-4** 🔑 **설비 해소 양갈래**: 팝업·트리 안내에 "점검표를 입력하거나, **실제 설치되지 않은
  설비라면 1.4 설비 대장에서 체크를 해제**하세요" — 체크만 있고 입력 없는 설비가 문서에 잘못된
  빈칸/기본값으로 나가는 것을 사용자가 알고 해소하게 한다.
  ⚠ 1.4 딥링크는 같은 경로 `?tab=` Link가 서버를 안 깨우는 함정 → 전체 이동 `<a>`.
- **S2-5** 트리(`plan-annex-sheet-tree.tsx`) 요약줄에 `● 필수 미입력 N건`(amber),
  `onBlankCount(설비n, 항목comp)` 시그니처 확장.

### S3 — 3층: 완료 보류 (하드)

- **S3-1** `inspection-step-sync.ts`: 완료 전환 유일 지점(applyStepSideEffects)에서
  **설치 시트의 범위 내 무응답 항목 > 0**(작동·종합 공통, §0)이면 completed 전환 **보류**(in_progress 유지).
  판정은 호출부(syncInspectionSteps)가 임박 시에만 계산(비용 게이트) → `holdCompletion` 인자.
  이미 completed인 회차는 소급하지 않는다.
- **S3-2** 보류 고지 채널: completeStep 경로(달력·슬라이드패널) alert + 점검 상세 워크벤치 배너
  (단계 전부 ✓ + status 진행중 모순 해소, 입력 화면 딥링크 포함).
- **S3-3** 차단 영향 실측(가드 blast-radius 규칙): 켜기 전 진행중 종합 회차 중 ● 무응답 보유
  건수를 스테이징에서 실측·보고.

### S4 — 문서(PDF·엑셀) 반영

- **S4-1** `report9-assemble.ts` missing에 `점검표 항목 미입력 N건(설치 설비[, 종합 필수 ● M건 포함])`
  추가 — 작동·종합 공통(§0), **설치 매칭 시트**(sheetMatchesFacilities) 한정.
  한 줄로 자동 전파: 별지 9호·4호 PDF 생성 missing + 미리보기 하이라이트 + 워크북 X-Workbook-Missing.
- **S4-2** 워크북 헤더 600자 절단 대비 — 생존 순위 조정(중복 고지 금지: r9.missing과 한쪽만).

## 5. 스위트 영향 (사전 실측)

- test-sheet-overview: 필드 추가는 additive — 무수정 통과. compBlank 단언 추가 예정.
- test-second-round-operational: 순수 함수(sheetScope/isItemInScope) 무변경 — 무영향.
- test-sheet-mother-drawer: 픽스처 전부 작동 회차 — 신규 UI 미발화. '저장' 문자열 버튼 금지(P19·P11).
- test-sheet-entry-page: 종합 회차 사용 — 기존 문구 보존 필수, dialog auto-accept=진행이라 안전.

## 6. 위험

- 보류 회차는 '진행중' 유지 → 9호 미제출 넛지·지난 회차 복사(completed 필터)가 늦어짐 — 배너에 출구 필수.
- patchLocal/overlaidProgress 재계산 누락 시 카운터 낡음 — 두 곳 모두 수정.
- 보류 판정 3왕복이 보류 상태 매 저장마다 반복(카탈로그 캐시로 경미).
