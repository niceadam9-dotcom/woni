# 소방계획서_45 — 점검표 모두 합격이면 ⑤⑥ 생략 (판정축을 「불량내역 0건」 → 「점검표 모두 합격」)

작성 2026-09-07 · 갱신 **2026-09-09**. 추적 정본: `erp_goal/소방계획서_45.json`.
상태: **독립 판정 3회(각 4인) · 제품 결함 22건 수리 · 유예분·인계 착수 완료 — 미커밋**(§7~§13).
`07b5d75`(1·2차분)는 **이미 푸시·운영 배포됐다**(16회차 `1af1abc`에 동반 — 이 문서의 종전 「미커밋」은 그 시점 기록이다).
발단 화면: `erp_goal/image-74.png` (점검 작업대 스텝바 — ④ 선택·⑤⑥ 흐림).

> ⚠ 채번: 42·43·44를 같은 날 오후 타 세션들이 15분 사이에 차례로 선점했다. 덮어쓰지 않고 45로 잡는다
> (빈 파일을 먼저 만들어 선점한 뒤 채웠다 — 확인 후 쓰기 사이에 또 뺏겼기 때문).

---

## 1. 발단 (사용자 요구, 2026-09-07)

> "점검표 모두 합격이면 4~6단계 생략합니다. 프로세스 제안해줘"

조사 결과 **⑤⑥ 생략은 이미 구현돼 있었다** — `activeStepNums(isSpecial, hasDefects)`가
`hasDefects ? [1..6] : [1,2,3,4]`를 돌려준다(`inspection-step-status.ts`). 그래서 요구의 실질이 갈렸고,
착수 전에 사용자에게 물어 확정했다.

| 물음 | 확정 |
|---|---|
| ④ 소방서 제출(별지 9호)도 생략인가 | **아니오 — ⑤⑥만.** 별지 9호는 점검 종료 후 15일 내 보고가 불량 유무와 **무관하게** 의무다 |
| '모두 합격'의 판정축 | **✕ 0건 AND 등록 불량 0건.** 필수 미입력 여부는 판정에 넣지 않는다 |
| 표시 | **현행 유지** — 칩은 남기고 `disabled` + 흐림 + '해당없음', 진행률 분모에서 제외 |

## 2. ⭐그래서 이 차수가 실제로 고친 것 — 「덜 생략」이다

종전 `hasDefects`는 **등록된 `inspection_defects`가 0건인가**만 봤다. 점검자가 점검표에 ✕를 찍어도
[불량(X) N건 — 표준 문구로 불량내역에 등록] 버튼(`inspection-sheet-client.tsx`)을 누르기 전에는 0건이다.
그 구간에서 ⑤⑥이 **"해당없음 — 불량 0건"으로 잠겼다** — 조치가 필요한 회차에서 화면이 "조치할 것이 없다"고 말한 것이다.

축을 사용자가 말한 대로 **점검표 모두 합격**(✕ 0 AND 불량 0)으로 바꾸면 그 구멍이 닫힌다.
**⑤⑥을 더 많이 생략하는 변경이 아니라, 잘못 생략하던 것을 되살리는 변경**이다 — 분모가 4→6으로 *늘어나는* 케이스가 새로 생긴다.

## 3. 착수 전 실측 — 소급 강등 위험(R-1)

분모가 늘면 ✕>0·불량 0건인 **기존 `completed` 회차**가 상세 진입만으로 `in_progress`로 되돌려진다
(`inspection-step-sync.ts` "되돌림에 예외 없음"). 사용자에게는 "멀쩡하던 완료가 저절로 풀렸다"로 보인다.
그래서 코드를 한 줄 고치기 전에 세었다 — `erp/scripts/_probe-45-xpass.mts`:

| | 자체점검 | ✕>0 · 불량 0 | 그중 completed |
|---|---|---|---|
| STAGING | 27건 | **0건** | **0건** |
| PRODUCTION | 3건 | **0건** | **0건** |

양쪽 0이므로 완화책 없이 진행했다. **이 순서를 뒤집었다면(먼저 고치고 나중에 세기) 되돌릴 수 없었다.**

> 2026-09-08 판정자 재실행: STAGING은 **27건**(최초 기록 28은 데이터 표류로 이미 썩은 수치 — 결론 0/0은 무손상).

## 4. 설계

### S1 — 판정 원천 `src/lib/inspection-step-status.ts` (순수)

- `StepEvidence`에 `sheetX: number` 신설. `defectsTotal`과 **다른 축**이다(등록 전 vs 등록 후).
- `hasSheetDefect({defectsTotal, sheetX})` 신설 — OR 합성의 **단일 원천**. 호출부가 제각기 `> 0`을 쓰면 화면이 갈라진다.
- `activeStepNums`의 **시그니처는 유지**(2인자 boolean), 인자명만 `hasDefects` → `needsRepairSteps`.
  옵션 객체로 바꾸면 `test-inspection-steps-sync`·`_judge2-r4-mutant`·`_judge2-r4-undo`의 단언 12곳이 동시에 깨지고 얻는 것이 없다.
- ⚠ **`evidenceDone` ⑤ 판정은 넓히지 않는다.** ⑤ 완료는 여전히 `defectsTotal > 0 && 전건 조치`다.
  넓히면 `defectsTotal === 0`에서 `0 >= 0`이 참이 되어 **하지 않은 일이 완료로 남는다**(D34-2).

### S2 — 서버 증거 수집 `src/lib/inspection-step-sync.ts`

`gatherStepEvidence`의 기존 `Promise.all`에 `count:'exact', head:true` + `.eq('result','X')` 한 줄.
`head:true`라 **행을 한 줄도 전송하지 않아** 1000행 상한과 무관하고, 병렬 배열에 얹으므로 왕복 지연도 늘지 않는다.
`syncInspectionSteps` 전 호출부(`sheet-actions.ts` 6곳·`actions.ts`·`step-revalidate.ts`)가 **추가 배선 없이** 새 판정을 따른다.

### S3 — 작업대 `src/components/inspections/inspection-workbench.tsx`

`TimelineData`는 건드리지 않는다 — `data.evidence`(StepEvidence)가 이미 흐르고 있어 **prop 배선 0줄**이고,
그 선택이 타 세션의 `TimelineData.certReported`(② 배치신고 전환)와의 충돌을 원천 회피한다.

- `needsRepairSteps` = `hasSheetDefect({defectsTotal: defectStat.total, sheetX})` → 칩 `na`·분모.
  `hasDefects`(등록분)는 **⑤ 완료 판정 폴백에 그대로 남긴다** — 두 축을 한 이름으로 합치면 ⑤가 잘못 완료된다.
- 문구 `'해당없음 — 불량 0건'` → `DOC_TERMS.naAllPass`(= `'해당없음 — 점검표 모두 합격'`).
- **④ 한 줄 안내(신규)**: 모두 합격일 때 ④ '제출 전제'에 "별지 10·11호는 해당없음 / 9호는 15일 내 의무".
  ④를 존치하기로 한 이상 **뒤가 왜 비었는지**가 ④ 화면에서 읽혀야 한다.
- **⑤ 미등록 ✕ 배너(신규)**: `sheetX > 0 && 불량 0`일 때 ⑤ pane 상단에 안내 + `[① 점검표에서 불량 등록하기]`(`setSel('checklist')`).
  등록 자체는 ①에서 한다 — 거기가 **등록될 표준 문구를 보고 판단하는 자리**라 호출자를 늘리지 않는다.

### S4 — 목록 `src/app/(dashboard)/inspections/page.tsx`

`?per_page=0`이면 `ids`가 무제한이라 점검당 count 병렬은 부적합 → `fetchAllRows` + `.eq('result','X')` + `.order('id')`.
**기존 `inspection_defects` 조회도 함께 감쌌다** — 한쪽만 감싸면 같은 화면에서 한 축은 정확하고 다른 축은 잘린, 더 나쁜 상태가 된다.

### S5 — 제출 현황판·고객 문서 현황

10·11호 '해당없음' 판정이 `defectsTotal === 0`으로 **네 곳에 흩어져** 있었다(표 2 + 엑셀 내보내기 2).
`allPassRow(r)` 모듈 상수 하나로 접고 `sheetX`를 축에 더했다. `customer-docs.tsx`도 같은 축·같은 문구
(`DOC_TERMS.naAllPass` 단일 원천 — `doc-requirements.ts` 머리주석이 이미 "화면 라벨은 반드시 여기서만"이라 선언한다).

### S6 — 범위 밖(후속 판단)

`customer-list.ts`(고객 목록 문서 스트립 `a10/a11`)·`plan-annex-section.tsx`·`plan-annex-round-card.tsx`도 같은 축을 쓴다.

**의도적으로 손대지 않은 것**(판정축이 아니다): `cron/defect-action-notify`(등록된 불량의 이행기한만 대상 — ✕만
있으면 기한 자체가 없다) · `bundle-actions.ts`(빈 서식 혼입 방지가 명시된 의도) · `report9-assemble.ts`(문서 조립용 목록).

## 5. 검증

| 축 | 결과 |
|---|---|
| `tsc --noEmit` | **0** (판정자 독립 재실행 rc=0·출력 0바이트) |
| `next build` | ⚠**미검증** — §8 잔여 참조(환경 제약) |
| `test-inspection-steps-sync.mts` (순수+실주행) | **71/71** (45 최초 9단언 + R-3 가드 4단언) |
| `_probe-45-sync-x.mts` (스테이징 DB 왕복 7시나리오) | **15/15** |
| `_probe-r4-defects.mjs` | **41/42** (D7 신규 16단언 포함) — 잔여 1건은 **기준선에서도 붉다** |
| `_judge2-r4-static.mjs` | 17/19 — 잔여 2건 **기준선 동일**(HEAD 16/19) |
| eslint (변경 13파일) | 신규 0. 남은 **error 6건**은 전부 장기 기준선 |

> ⚠ 최초 기록의 `next build rc=0 / 32.2s`는 이 문서에서 **철회한다**. 판정자가 재현하지 못했고
> (락 경합·시간 문제로 미실행), 2026-09-08 수리 뒤에는 여유 RAM 1.54GB라 돌릴 수 없었다.
> ⚠ eslint는 「5건 · 전부 타 세션 ② 배치신고 전환」이라 적었으나 실측 **6건**이고 귀속도 틀렸다 —
> 6건 모두 ②와 무관한 장기 기준선(`Date.now` 순수성 · `<a>` 링크 · set-state-in-effect 4)이다.

### ⭐판별식은 개수가 아니라 되돌림 경로다

"분모가 6이다"만 세면 **종전 축도 통과한다**(불량이 등록돼 있으면 어느 축이든 6이다).
진짜 판별식은 `_probe-45-sync-x.mts` 5번 — **불량내역만 삭제하고 ✕는 남긴 상태**다.
종전 축은 여기서 조용히 '해당없음'으로 돌아갔고, 새 축은 ①~⑥을 유지한다.

> ⚠ **정정(2026-09-08):** 최초 기록의 「5번이 **유일한** 판별식」은 거짓이다. 종전 축을 대입해
> 7시나리오를 전수로 따지면 판별식은 **3개**다 — **2번**(✕1·불량 0. 종전 축은 `[1,2,3,4]`를 내
> 실패하고, 5번보다 단순하며 **앞에 온다**) · **5번** · **6번**(✕가 남아 `completed`로 안 올라간다 —
> 사용자에게 보이는 결과까지 가므로 **셋 중 가장 강하다**). 근거로 적은 「분모만 세면 종전 축도
> 통과한다」도 4·7번에만 참이다. 결론(5번은 판별식이다)은 옳았지만 **「유일」과 그 논거가 과장**이었다.

### 기준선을 먼저 찍었다

정적 프로브 3건이 붉었는데, `git worktree add --detach HEAD`로 **깨끗한 HEAD에서 같은 프로브를 돌려** 갈랐다:

- `종이 보관 기록이 sync를 부른다` — HEAD에서도 붉다. **내 것 아님.**
- `작업대 철회 버튼이 isSpecial 게이트 안에` — HEAD에서도 붉다. **내 것 아님.**
- `작업대가 evidenceDone으로 ✓를 계산한다` — 정규식이 `evidenceDone(data.evidence)`를 찾는데
  코드는 S3-6 선반영 때문에 **스프레드로 부른다**(`evidenceDone({ ...data.evidence, … })`). 정규식이 썩은 것이라 고쳤다.

> ⚠ `_judge2-r4-static.mjs`는 untracked라 워크트리에 없다 — **복사해 넣어야** 기준선을 잴 수 있다.

**⚠ 판정자가 이 절에서 세 가지를 바로잡았다(2026-09-08):**

1. **인과가 틀렸다.** 「`recordCertPaperAction`을 타 세션 ② 전환이 **지웠다**」고 적었으나, 그 함수는
   작업트리 `timeline-actions.ts:117`에 **살아 있다**(HEAD에는 없다). 붉음의 정체는 '삭제돼서'가 아니라
   **「살아 있는데 `syncInspectionSteps`를 안 부른다」**다 — 45 책임은 아니지만 성격이 다르고,
   그쪽 세션에 인계할 값어치가 있다.
2. **세 번째 항목은 앞의 둘과 성격이 다르다(conflation).** 앞의 둘은 프로브 버전과 무관한 **코드 사실**인데,
   이것은 **프로브 버전에만 존재하는 측정 인공물**이다. 동일 프로브로 재면 HEAD에서 **초록**이므로
   「기준선에서도 붉었다」가 이 항목에는 성립하지 않는다. 수리 자체는 정당하다
   (`git show HEAD:`로 HEAD 원문이 새 정규식엔 매치되고 구 정규식엔 안 매치됨을 확증).
3. **대조의 분모가 어긋났다.** `25/26 vs HEAD 23/25`는 **프로브 버전도 분모도 다른** 대조다
   (HEAD 프로브는 25단언·썩은 정규식 보유). 동일 프로브 기준선은 **23/26**이다.
   결론은 안 바뀌지만, 프로브를 고치면서 기준선을 재면 '내 수리로 초록이 된 것'과 '원래 초록이던 것'이 섞인다.

또 「썩은 정규식 **2건**」 중 입증 가능한 건 **1건뿐**이다 — `_judge2-r4-static.mjs`는 untracked라
git 이력이 없어 과거에 썩은 형태였는지 확인할 수 없다. 근거 줄번호도 썩어 있었다(47→**51**, 53→**55**).

## 6. 이번 차수의 함정 3가지

- ⭐**주석에 적기만 해도 계수에 걸린다.** `_judge2-r4-static`은 `data.responded > 0`의 **개수가 1**임을 단언한다
  (리터럴 판정이 폴백 밖으로 번지는 것을 막는 가드). ④ 안내 조건에 그 표현을 쓰다가 걸렸고, `doneByNum[1]`로 고쳤는데
  **그 사연을 적은 주석이 다시 계수에 걸렸다.** 문자열 계수 가드 근처에서는 주석 문구까지 축이다.
- ⚠ **`_judge2-r4-static`의 `grep`은 `git`을 PATH에서 찾는다.** 이 환경의 PATH git은 죽은 경로라
  `execSync`가 실패하고 `catch`가 `[]`를 돌려준다 — "UPDATE하는 파일은 하나" 단언은 `[]`로 **붉게** 뜨고,
  반대로 "죽은 UI를 렌더하는 곳이 없다" 단언은 `[]`로 **공허 통과**한다. 같은 고장이 방향에 따라 오탐·미탐 둘 다 만든다.
  실행 전 `$env:PATH = 'F:\AI\tools\MinGit\cmd;' + $env:PATH`.
- ⚠ **`activeStepNums`의 2번째 인자를 `hasDefects`로 못 박은 정적 정규식이 2개** 있다. 리네임하면 함께 갱신해야 한다
  (이름이 이제 거짓말이므로 프로브를 고치는 쪽이 정직하다).

## 7. 독립 판정 (2026-09-08, 4인 축 분할)

축을 **명시 분할**해 띄웠다 — 나누지 않으면 두 판정이 통째로 중복된다(`feedback_check_concurrent_session`).
J1 = S0·S1·S2 / J2 = S3 / J3 = S4·S5 + **누락 표면 전수** / J4 = S6 검증 주장 **재실행**.
각자에게 "evidence가 실재하는가"가 아니라 **"반증을 시도하라"**고 지시했다.

축 분할이 값어치를 했다: J1·J2가 **독립적으로 같은 결함**(§8-1)에 도달했고, J3만이 문서에 **존재조차
없던 표면 2개**를 찾았고, J4만이 검증 주장 7건의 거짓을 갈랐다.

### ⭐이번엔 제품이 틀렸다

37·38차수의 독립 판정은 「제품은 옳고 문서·검사가 틀렸다」로 끝났다. 이번은 다르다 — **제품 결함 4건**.
차이는 이번이 **기존 축을 넓힌** 변경이었다는 점이다. 신규 기능은 이웃이 없지만, **축 확장은 이웃이 전부다.**
잡힌 4건이 하나도 "판정 로직이 틀렸다"가 아니라 전부 **"넓힌 자리를 이웃이 못 따라왔다"**였다.

## 8. 판정이 잡은 것과 수리 (§S8 상세)

| | 결함 | 성격 |
|---|---|---|
| **8-1** 🎯 | `✕>0·등록 불량 0`에서 ⑤ **[사유 완료]가 영구 고정** — `isForced5Void`가 `0 < 0`이라 안 걸린다. ⑥·`status=completed`까지 간다 | **이번 차수가 만든 도달성.** 종전엔 칩이 `disabled`라 도달 불가였다 |
| **8-2** | 1000행 포장을 **셋 중 둘**에만 — 목록 `inspection_steps`(분모·분자의 원천!) · 현황판 `inspection_defects`(잘리면 **거짓 '해당없음'**) | 자기가 세운 원칙을 자기가 깼다 |
| **8-3** 🎯 | 마감 알림 크론에 `activeStepNums` 필터가 **아예 없다** — 화면은 '해당없음', 알림은 '마감 임박' | **문서에 언급조차 없던 표면** |
| **8-4** | 고객 상세 진행바가 6행을 그대로 세어 영원히 **4/6**(목록은 4/4) | **문서에 언급조차 없던 표면 2** |
| 8-5~8-10 | 미렌더 파일의 문구 갈라짐 · ④ 문장 자기반복 · CTA 권한 누락 · 조용한 폴백 · Q-4 별지 2곳 · **소스 주석의 거짓** | 표기·짝 맞추기 |

### ⭐지적을 받아도 조건은 내가 다시 따진다

판정자는 `isForced5Void`를 `defectsDone < defectsTotal || sheetX > 0`으로 넓히라고 제안했다.
**그대로 썼으면 반대쪽 결함을 만들 뻔했다** — 불량을 등록해도 ✕ 응답은 남으므로
(`createDefectsFromXAction`이 응답을 지우지 않는다) ⑤ 사유 완료가 **영구 무효**가 된다.
'미등록 ✕'는 `defectsTotal === 0`일 때다(작업대 `xUnregistered`와 같은 축). 그래서 조건을 좁혀 넣고,
**등록 후 전건 조치했으면 ✕가 남아 있어도 유효**라는 반대 방향 단언을 함께 세웠다.

### ⭐검사 67개가 전부 눈이 멀 수 있다

§8-1은 순수 단언 67개도, DB 왕복 15/15도(6번이 하필 ⑤가 아니라 **②**에 강제완료를 건다),
정적 프로브 2종도 통과시켰다. 분모를 세는 단언은 분모만 본다.
**"다 초록이다"는 "그 구멍을 봤다"가 아니다.** 신규 가드 4단언은 구 구현에서 `0 < 0 === true`를
요구하므로 확실히 물린다(공유 트리라 실제 뮤테이션은 미실시 — 이 판정은 **추론**이다).

## 8-2. 2차 독립 판정 (2026-09-08, 4인 축 분할) — §S9 상세

§8 수리분 10건을 판정했다: **8 PASS · S8-2·S8-4 PARTIAL · 신규 제품 결함 6건.**
축 분할은 J5=S8-1·6·7 / J6=S8-2·8 / J7=S8-3·4 / J8=S8-5·9·10 + 이웃 전수 수색.

**⭐⭐잡힌 것의 성격이 1차와 또 달랐다 — 내가 방금 세운 규칙을 내가 방금 쓴 코드가 어겼다.**
목록 페이지에서 「1000행 포장을 셋 중 둘에만 적용했다」를 **자경 주석까지 달아** 고쳐 놓고,
같은 물결에 새로 쓴 고객 상세에서 **똑같이 둘만 감쌌다**(판정자 3인 독립 합치). 조용한 폴백
표면화도 같은 자리에 안 갔다. **수리 직후가 가장 위험하다** — 규칙을 방금 말했다는 이유로 지켰다고 느낀다.

**⭐표면화는 절반이었다.** `console.error`를 찍고 "조용한 폴백을 고쳤다"고 적었으나 실패해도 화면은
여전히 틀린 값을 보였고, 기울기가 **위험한 쪽**이었다(목록은 ⑤⑥이 사라져 초록 4/4, 현황판은
`allPassRow`가 뒤집혀 **법정 서식에 거짓 「해당없음」 인쇄**). 크론에는 내가 이미 보수 판정을
써 뒀다 — **대조군이 같은 차수 안에 있었다.**

**⭐개수 비교는 집합 연산의 근사일 뿐이다(R-5).** '미등록 ✕'를 `defectsTotal === 0`으로 근사한 탓에
불량이 한 건이라도 있으면 미등록 ✕가 통째로 안 보였다. `deleteDefectAction`이 불량 행만 지우고
✕는 남기므로 3건 등록→2건 삭제→남은 1건 조치하면 ⑤⑥·`completed`까지 갔다. **집합 차**
(`unregisteredX` = ✕ `item_code` − 불량 `defect_code`)로 바꾸니 삭제·수기추가·재기재가 한 규칙으로 덮였다.

**⭐붉은 단언의 「원인 진술」도 실측 대상이다.** `recordCertPaperAction`이 sync를 안 부른다고 **두 번
서술하면서 한 번도 그 함수를 열지 않았다** — 살아 있었고 래퍼를 거칠 뿐이었다. 낡은 건 정규식이었다.

**⭐가드가 무는지는 추론하지 말고 재라.** §S8-1이 "추론이다"로 남긴 자리를, 제품을 뮤테이션하지 않고
**구 구현식을 옮긴 통제 프로브**(`scripts/_45-discriminant.mjs`)로 실측했다 — **판별식 2/3**.

**사용자 확정 3건**: Q-6 화면 쌍둥이 4곳은 **별도 차수 유예** · Q-7 **집합 차로 정밀화** ·
Q-8 번들 체크리스트는 **현행 유지**(10호는 등록 불량 없으면 빈 서식).

**재측정**: tsc rc=0·0바이트 · 스위트 **76/76** · `_probe-r4-defects` **44/44** ·
`_judge2-r4-static` **19/19** · 스테이징 DB 왕복 **15/15** · 통제 프로브 판별식 2/3.
⚠뒤의 두 수치에는 내가 고친 단언 3건이 포함된다 — 셋 다 **제품 원문을 먼저 읽어** 제품이 옳음을
확인한 뒤 단언을 고쳤다(§S9-9).

## 8-3. 3차 독립 판정 (2026-09-09, 4인 축 분할) — §S10 상세

S9 수리분 7건을 판정했다: **S9-1·S9-5 PASS · 나머지 5건 PARTIAL · 신규 제품 결함 12건(중대 4).**
축 분할은 J9=S9-1·2 / J10=S9-3 / J11=S9-4·5 / J12=S9-6·7 + 이웃 전수 수색.

PARTIAL 5건은 전부 같은 문장이었다 — **「주장한 수리는 원문 대조로 참이나, 같은 결함이 다른
자리에서 다시 성립한다」**. 판정을 그대로 `partial`로 남긴 이유가 여기 있다(S10-9).

### ⭐⭐⭐세 번 다 같은 형태였다면 그것은 실수가 아니라 구조다

1차 「축을 넓혔더니 이웃이 못 따라왔다」 · 2차 「방금 세운 규칙을 방금 쓴 코드가 어겼다」 ·
3차 「그 수리 커밋 **자신 안에서** 또 재생산됐다」. 세 번째에 와서야 원인이 보였다 —
**판정 로직은 공유돼 있었는데 그 판정에 넘길 값을 모으는 일이 공유되지 않았다.**
`activeStepNums`는 순수 함수로 한 벌이었지만, 불량·✕를 조회해 그 인자를 만드는 코드는
표면마다 각자였고 **다섯 표면은 아예 만들지 않았다**. 순수 함수만 공유하는 것으로는 부족하다.
→ `lib/active-steps.ts`로 조회 계층을 승격했다(§S11-0). 그리고 사람이 세 번 찾은 것을
검사로 내렸다(§S13, `_probe-45-neighbors.mjs` — 깨끗한 HEAD 대조군 **4/47** vs 내 트리 **47/47**).

### 🎯중대 4건

| | 결함 | 왜 무거운가 |
|---|---|---|
| **R-1** | `gatherStepEvidence`가 보수 판정을 `unregisteredX` **한 필드에만** 걸고 형제 `sheetX`는 실패 시 0으로 접혔다 | ⑤가 활성 집합에서 빠지는 순간 보수 판정한 값을 **아무도 안 읽는다**. 끝은 화면이 아니라 `inspections.status='completed'` **DB 쓰기**다 |
| **R-4** | 크론 `inspections` 조회만 맨몸 — 「셋 중 둘」이 **수리 커밋 자신 안에서 3번째로** 재생산 | ⑤⑥ 필터가 **조용히 전면 무효**(수리 이전 복귀)인데 `skippedNa`·로그 어디에도 안 남는다 |
| **R-5** | 상류 상한을 20배로 풀며 하류 멱등 조회를 안 풀었다 | 임계가 **단계 200건 수준**(단계×수신자) → **중복 알림 대량 발송**. S9-6 수리가 만든 신규 노출 |
| **R-6** | ⑤⑥ 모순이 남은 **5번째 화면** `/my/schedules` | Q-6 유예 4곳 **밖**이었다 — 유예 목록은 전수가 아니었다 |

**2인 독립 합치**: `docs-actions.ts`의 별지 트리 원천이 `error`를 한 번도 안 읽어 거짓
「해당없음」을 그린다(J9·J10이 각각 도달). 같은 커밋이 **같은 파일의** 현황판에는
`allPassUnknown`을 신설했으면서 조립 함수에는 안 갔다.

### ⭐실데이터가 픽스처를 이겼다 — `defect_code`가 null인 등록 경로

수기 폼은 코드 칸이 「선택」이고 모바일 Edge Function은 **아예 안 보낸다**. 집합 차는 null을
통째로 버리므로 **등록했는데도 미등록 ✕로 셌다** — 스테이징 9행 중 **2행(22%)**에 이미 발현 중이었고,
그 회차는 ⑤가 영구 미완이었다(출구가 배너 CTA뿐인데 누르면 별지 9·10·11호에 **중복 인쇄**).
사용자 확정 **Q-9 = 개수 상쇄**. 그리고 J11이 함께 잡은 것 — **76/76 초록이 집합 차를 한 번도
실행한 적이 없었다**(실주행이 `result:'X'`를 한 건도 안 만들었다). 그 무커버리지 구간에
정확히 이 결함이 있었다([[feedback_fixture_distribution_blind]] 재발).

### ⭐포장했는가만 묻고 요청이 나가는가를 아무도 묻지 않았다

인계 항목 「`.in()`은 URL 길이를 풀지 않는다(414 **가능**, 미실측)」을 재 봤다 —
**200건 통과 / 400건부터 실패**, 5000건에서 nginx `414 Request-URI Too Large` 원문.
가능성이 아니라 **실재**이고, **1000행 상한보다 앞에 있는 벽**이다. 추정치로 적힌 '~8KB 한도'도
실제 경계(7.5~15KB 사이)와 달랐다 — 미실측 항목은 가설이지 사실이 아니다.

## 8-4. 4차 독립 판정 (2026-09-09, 4인 축 분할) — §S14 상세

**배포 직전에 돌렸다.** 지난 세 판정이 매번 중대 결함을 찾았고 S10~S13은 아무도 반증하지 않은
상태였다 — 배포는 되돌리기 어려운 단계라 순서를 그렇게 잡았다. 결과: **제품 결함 8건(중대 3) +
검사 결함 5건.** 배포하지 않기를 잘했다.

### 🚨⭐⭐⭐초록은 물린다는 증거가 아니다 — 판정자가 내 검사를 반증했다

「깨끗한 HEAD 대조군 4/47이니 **43건이 문다**」고 적었는데, J16이 제품을 되돌린
**37개 변이 중 24개에서 프로브가 초록**이었다. 대조군 대조는 「이 검사가 **무언가**를 잡는다」만
보여줄 뿐 「**이것**을 잡는다」를 보여주지 않는다.

원인은 전부 하나 — **문자열이 어딘가에 있는지만 봤다.**

| 뚫린 변이 | 왜 안 물었나 |
|---|---|
| `hasSheetDefect`의 `if (e.axisIncomplete) return true` 삭제 | 정규식이 **형제 함수의 같은 줄**에 매치. DB에 `completed`를 쓰게 하던 바로 그 가드다 |
| 「셋 중 둘」을 세 파일에서 재현 | 파일당 **1회 등장**만 봤다 — 이 프로브의 존재 이유가 그 형태인데 |
| 「전체」 보기를 통째로 원복 | **삼항의 머리**(`pageSize > 0 ? query`)만 봤다 |
| `IN_CHUNK_SIZE` 150 → 1500 | **값을 베껴 적어** 범위를 안 봤다 |

수리: `count()`(정확히 n회) · `inFn()`(함수 본문을 잘라 형제 매치 차단) · else 가지의 실체를 보는
단언 · 값 대신 범위. **47 → 81단언.** 그리고 초록을 믿지 않고 **내가 직접 변이 실험**을 했더니
(`scripts/_45-mutate.mjs`) 첫 실행에서 **또 하나가 뚫렸다**(사이드바 차감을 죽여도 초록 — 호출문
존재만 봤다). 최종 **12/12 문다 · 무는 척 0 · 측정불가 0**.

> ⚠ 한 건이 처음에 「패턴 불일치」로 나왔다(CRLF). **측정 못 한 채로 두지 않고** 정규식을 고쳐 다시
> 쟀다 — 측정불가를 통과로 읽으면 그것이 곧 공허 통과다.

### 🎯중대 3건

| | 결함 | 왜 무거운가 |
|---|---|---|
| **R-1** | 정본 화면(작업대)이 `axisIncomplete`를 **손에 쥐고 버렸다**(2인 합치) | ✕·불량 조회 실패 시 `sheetX`·`unregisteredX`가 **둘 다 0으로 접혀** 신호를 나르는 건 그 축 하나뿐인데, 주 표면이 그걸 버렸다 — 서버는 보수 판정하는데 **화면만 4/4 100%** |
| **R-8** | **6번째 표면 = 모바일 앱** | 6행을 그리고 ⑤⑥에 [완료] 버튼을 띄우며 `status='completed'`를 직접 쓴다. 웹이 막아 온 끝인데 여기는 **사람이 누른다** |
| **R-2** | 「셋 중 둘」이 **네 번째**로 재생산 | `fetchAllRowsByIds`를 6곳에 깔며 크론 멱등 조회만 빠뜨렸고, 3차에 넣은 `continue`가 그 실패를 **전량 미발송으로 증폭**(방향만 뒤집히고 벽은 그대로) |

### ⭐타입을 조이는 자리를 잘못 고르면 옆문이 그대로 남는다

「필수 필드로 만들면 빠뜨릴 수 없다」고 생각해 `StepEvidence`를 봤는데, J13이 타입 정의 원문으로
지적했다 — 그 축을 읽는 `hasSheetDefect`의 인자는 `StepEvidence`가 아니라 **구조적 부분타입**이라,
원본을 필수로 바꿔도 호출부가 **새로 만든 리터럴**은 tsc가 통과시킨다. **인자 타입** 쪽을
`boolean | undefined`(필수 키)로 조이자 tsc가 결함 호출부 2곳을 정확히 집어냈다.

### ⭐「범위 밖」은 축마다 다르다

모바일을 놓친 이유가 「범위 밖이라서」가 아니었다 — **같은 차수가 Q-9에서 이미 모바일을 감사했다**
(`functions/add-defect`의 `defect_code`). **불량 축은 봤고 단계 축은 안 봤다.**
전수 수색은 「어느 패키지까지」가 아니라 **「어느 축으로」**를 먼저 정해야 한다.

### 반증에 살아남은 것도 기록한다

J15는 「`like.special_*`의 `_`가 와일드카드라 정기가 샌다」는 가설을 **실측으로 반증**했다 —
서버 술어는 넓어지는 게 아니라 **좁아지고**(최소 8자 요구), 차집합은 값 `'special'` 하나뿐인데
그런 행이 스테이징·운영 어디에도 없다(25/25·3/3). 다만 「이중 방어」라는 **주석은 거짓**이었다
(서버 ⊊ 클라라 그 filter는 한 행도 못 거른다) — 사실은 고치고 줄은 남겼다.

## 9. 잔여

- **`next build` 게이트 · `test-timeline.mts`** — 환경 제약으로 미실시. 측정 시점 여유 RAM **1.54GB**/15.57GB,
  타 세션 dev 서버로 보이는 node 1개가 **3.9GB**, `.next` **5.9GB**. 이 상태의 빌드는 OOM·시스템 무응답
  위험이 크고(`risk_dev_postcss_worker_leak`: 2.6GB에서도 완전 무응답 실측), dev 재기동은 타 세션 작업을
  깨므로 **사용자 승인 대기**. tsc 0으로 갈음했으나 **두 축은 다르다** — 이번 변경은 크론 라우트·서버
  컴포넌트가 lib를 새로 import하므로 RSC 경계 검사와 무관하지 않다.
- **커밋·푸시·배포** — 사용자 승인 대기. 공유 작업트리에 타 세션 변경 **1151행** 미커밋이라
  **pathspec 명시 커밋만**(`risk_shared_index_commit`).
- ~~**§8 수리분(10건)의 독립 판정**~~ — ✅**완료(§8-2 / §S9)**. ~~**§S9 수리분(7건) 미판정**~~ — ✅**완료(§8-3 / §S10)**.
  대신 **§S10~S13(3차 수리분 12건 + 유예분 7 + 인계 3 + 검사 5)이 미판정**이다(구현자=나) — 4차의 몫이다.
  ⚠4차는 「축을 넓힌 자리의 이웃 / 방금 세운 규칙을 방금 쓴 코드가 어김 / 수리 커밋 자신 안의 재생산」
  **세 형태를 먼저 찾을 것** — 세 번 다 나왔다.
- ~~**`customer-list.ts` 고객 목록 스트립**~~ — ✅**해소(§S11-6)**. Q-4 유예를 사용자 지시로 해제하고 수리했다.
- ~~🚫**화면 쌍둥이 4곳(Q-6 유예 확정)**~~ — ✅**해소(§S11)**. 사용자 지시로 유예를 해제하고 **다섯 곳**을 수리했다
  (판정이 5번째 `/my/schedules`를 찾았다). 수리는 문서가 예고한 「크론 함수 재사용」이 아니라
  **`lib/active-steps.ts`로 승격**이었다 — 지역 함수로 둔 것이 애초에 다섯 표면을 눈멀게 한 원인이다.
  ⚠**⑤ [입력] 딥링크가 비활성 단계로 갈 때의 막다른 길은 남아 있다**(B-11로 이관).
- **`fetchAllRows`의 부분 행 반환**(B-11) — 오류 시에도 그때까지 받은 행을 돌려주는데 `error`를 안 보는
  소비부는 그것을 진실로 읽는다(6행 중 3행이면 진행바가 **3/3 = 100% 초록**). 헬퍼의 반환 규약을 바꾸면
  **전 호출부에 파급**되므로 착수 전에 범위를 먼저 재야 한다([[feedback_guard_blast_radius]]).
- **`allPassUnknown`의 '보류'가 '미생성'과 구별되지 않는다**(B-10) — 기울기는 안전한 쪽이라 미수리.
- **`fire_plan_gen_jobs` 정리 정책**(§S12-3) — 누적은 실측으로 확인(최대 중복 6행)했으나 규모가 작고
  (done 23행 = 상한의 2%) 보관 기간·중복 접기는 **사용자 결정 사항**이라 착수하지 않았다.
- 🚫**번들 체크리스트(Q-8 현행 유지)** — ✕만 있고 미등록이면 10·11호가 목록에서 사라진다.
  같은 화면의 ⑤⑥ 칩·별지 트리와 **알려진 갈라짐**.
- ~~**타 세션 인계 — `recordCertPaperAction`**~~ — 🚫**철회됨**. 배선은 살아 있고 래퍼
  `syncStepsAndRevalidate`를 거칠 뿐이다(`timeline-actions.ts:132`). 낡은 것은 정규식이었다(§S9-9).
- ~~**`_judge2`의 철회 단언 은퇴**~~ — ✅**해소됨**. 제품이 옳아서 **은퇴가 아니라 뒤집었다**(§S9-9) —
  지우면 이 설계가 되돌아가도 아무도 모른다.
- **인계 3건(2차 판정 부산물)** — 「전체」 보기(`?per_page=0`)가 1000건에서 조용히 잘린다 ·
  `.in()`은 요청 URL 길이를 풀지 않는다(414 가능, 미실측) · `fire_plan_gen_jobs`는 큐라 재생성마다 쌓인다.
