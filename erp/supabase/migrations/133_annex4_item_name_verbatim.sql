-- 133: 편입 항목명을 고시 원문 축자로 정정 (2026-08-13, 소방계획서_21 R5-7②)
--
-- 132가 [동력제어반] 표지 문장 3건을 한 형태(`하고,“…”표지`)로 통일해 넣었는데,
-- 고시 원문은 설비마다 공백이 다르다. `_probe-verbatim.mjs`로 대조한 결과 2건이 어긋났다.
--
--   2-H-021  원문 `…동력제어반”␣표지 설치 여부`      / 132 `…동력제어반”표지…`    → 공백 1
--   3-K-031  원문 `하고,␣“…”␣표지 설치 여부`         / 132 `하고,“…”표지…`         → 공백 2
--   13-G-031 원문 `…동력제어반”표지 설치 여부`        / 132 동일                    → 일치(손대지 않음)
--
-- 왜 원문을 따르는가: 이 문구는 우리가 쓰는 말이 아니라 **법정 서식에 인쇄되는 말**이다.
-- 공백 차이가 조판 흔들림일 수는 있지만 그렇게 단정할 근거가 우리에게 없고, 원문과 같게 두면
-- 고시가 개정돼도 대조가 기계적으로 된다(추출기로 diff가 그대로 돈다).
--
-- 나머지 10건은 이미 축자 일치라 건드리지 않는다.
-- 재실행 안전: 대상 문구일 때만 바꾸므로 두 번 실행해도 결과가 같다.

UPDATE inspection_sheet_items
   SET item_name = '앞면은 적색으로 하고,“옥내소화전설비용 동력제어반” 표지 설치 여부',
       updated_at = now()
 WHERE item_code = '2-H-021'
   AND item_name = '앞면은 적색으로 하고,“옥내소화전설비용 동력제어반”표지 설치 여부';

UPDATE inspection_sheet_items
   SET item_name = '앞면은 적색으로 하고, “스프링클러설비용 동력제어반” 표지 설치 여부',
       updated_at = now()
 WHERE item_code = '3-K-031'
   AND item_name = '앞면은 적색으로 하고,“스프링클러설비용 동력제어반”표지 설치 여부';

DO $$
DECLARE n INT;
BEGIN
  SELECT COUNT(*) INTO n FROM inspection_sheet_items
   WHERE (item_code = '2-H-021'  AND item_name = '앞면은 적색으로 하고,“옥내소화전설비용 동력제어반” 표지 설치 여부')
      OR (item_code = '3-K-031'  AND item_name = '앞면은 적색으로 하고, “스프링클러설비용 동력제어반” 표지 설치 여부');
  RAISE NOTICE '원문 축자 정정: %/2', n;
END $$;
