/** 화면에 붙이는 **법령 근거 참조** — 조문 이름과 원문 링크만 둔다 (2026-09-02).
 *
 *  ## ⭐ 규약: 우리가 조문을 요약해 화면에 쓰지 않는다
 *  요약하는 순간 그 문장은 우리 **주장**이 되고, 법이 개정되면 코드는 조용히 옛말을 계속 찍는다.
 *  이 저장소는 이미 같은 함정에 빠진 적이 있다 — `_doc01/*.MD`(파생 요약본)를 믿고 별지 서식을
 *  고칠 뻔했는데 hwpx 원문이 그 요약본을 반증했다. 그래서 여기 두는 것은 셋뿐이다:
 *    ① 조문 이름(무엇을 근거로 삼았는가)  ② 원문 링크(사람이 직접 확인)  ③ 확인일(언제 기준인가)
 *
 *  ## 확인일(asOf)을 반드시 둔다
 *  날짜가 없으면 낡았다는 사실을 아무도 알아챌 수 없다. 화면이 이 날짜를 함께 보여주므로
 *  사용자가 "1년 전 기준이네"를 스스로 판단할 수 있다.
 *
 *  ## 판정 규칙 자체는 여기 없다
 *  급수 산정 규칙은 `lib/fire-plan-suggest.ts`에 있고, 그 규칙은 **아직 원문 대조가 안 됐다**
 *  (2026-09-02: law.go.kr 별표4 PDF 추출 실패·로앤비 로그인 벽·협회 페이지 인코딩 깨짐, 저장소에도
 *  별표4 원문 자산 없음). 그래서 화면은 그 결과를 **'추천'으로만** 표기하고 확정은 사람이 한다.
 *  ⚠ 원문을 확보하면 `fire-plan-suggest`의 규칙을 대조하고, 그때 이 파일에 조문 인용을 추가할지
 *    판단할 것. 대조 전에 인용을 늘리지 말 것 — 틀린 근거는 없느니만 못하다.
 *
 *  ## 알려진 미대조 항목 (원문 확보 시 최우선 확인)
 *  · `fire-plan-suggest.ts` 1급 ② 주석은 "연면적 1만5천㎡ 이상(아파트·**연립** 제외)"인데
 *    코드의 `isApt`는 `/아파트|공동주택/`이라 **연립주택을 제외하지 않는다**.
 *    현재 데이터에 연립·다세대가 0동이라 실피해는 없지만(2026-09-02 실측), 주석과 코드가 어긋나 있다. */

export type LegalBasis = {
  /** 화면에 뜨는 짧은 이름 */
  label: string
  /** 무엇을 정하는 조문인가 */
  title: string
  /** 국가법령정보센터 원문 */
  url: string
  /** 이 링크·이름을 사람이 확인한 날 (YYYY-MM-DD) */
  asOf: string
}

/** 소방안전관리대상물 급수(특급·1급·2급·3급)의 근거 조문 */
export const GRADE_BASIS: LegalBasis = {
  label: '화재예방법 시행령 별표4',
  title: '소방안전관리대상물의 범위',
  // 국가법령정보센터 한글 경로 — 2026-09-02 응답 확인(법령명 일치). 별표는 본문 페이지의 [별표/서식]에서 연다.
  url: 'https://www.law.go.kr/법령/화재의예방및안전관리에관한법률시행령',
  asOf: '2026-09-02',
}
