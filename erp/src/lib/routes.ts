/** 앱의 기본 착륙지 — 로그인 직후·루트(`/`)·권한 부족 폴백이 **모두 여기로** 온다.
 *
 *  종전에는 `/dashboard` 문자열이 여섯 곳(app/page.tsx · login/actions.ts · proxy.ts×2 · auth.ts×2)에
 *  흩어져 있어, 착륙지를 바꾸려면 여섯 곳을 다 찾아야 했고 한 곳만 빠져도 "로그인하면 달력인데
 *  권한 없는 화면을 누르면 대시보드로 튄다"처럼 조용히 갈라졌다. 상수 하나로 못 박는다.
 *
 *  ⚠ 폴백지로 쓰이므로 **모든 역할이 접근 가능한 경로**여야 한다. 권한 게이트가 있는 경로를 넣으면
 *  튕긴 사용자가 다시 튕겨 무한 리다이렉트가 된다. 점검 달력은 게이트가 없다(2026-08-19 확인).
 *
 *  기본 화면을 점검 달력으로 (2026-08-19 사용자 확정) — 소방 점검 업무가 이 회사의 본업이고,
 *  대시보드보다 로드도 빠르다(실측: 직원 5.9s→3.3s, 관리자 7.0s→3.7s. dev 서버 상대 비교).
 */
export const HOME_PATH = '/inspections/calendar'
